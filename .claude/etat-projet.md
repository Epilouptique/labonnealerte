# LaBonneAlerte — État du projet (27 juillet 2026, fil "decks — catégories, points, boutique & vue liste" ; précédents : "cartes & decks", "vagues d'alertes & PanneauPocket")

Ce document fait foi pour tout nouveau fil. Il remplace toute version antérieure d'etat-projet.md (fichiers du projet Claude Chat ET .claude/ côté VS Code — synchroniser les deux ; pas de synchro automatique, remplacer la copie Claude Chat à la main).

## Le projet en une phrase

labonnealerte.fr (URL canonique = apex ; www redirige en 301) — le kiosque francophone d'alertes utiles : ~264 sources actives prêtes à l'emploi, activables en un clic, gratuites, open source, anti-spam par ADN. Standard ouvert OpenAlert v2 paramétrée (champ params). Public : francophonie entière (métropole, outre-mer, Québec, Belgique/Suisse, diasporas, expatriés). Édité par Hugo Vial-Jaime (Dahu Concept, Gap). Projet frère : DoomName (doomname.com), même compte Railway, première source externe paramétrée du kiosque.

## État du produit (~264 sources actives en prod ; 264 enabled dans init.sql = 245 socle + 18 cartes PanneauPocket curées (vague L) + cyclones-outremer activée ; orphelin spotify PURGÉ le 26/07 — base alignée sur init.sql)

> Inventaire détaillé et à jour du cycle "vagues" (juillet) : **rapports/synthese-etat-alertes-2026-07-23.md** (source de vérité des sources, familles + écartées + clés API). Les sections thématiques ci-dessous décrivent le socle historique ; le bloc VAGUES 2026 (fin de section) résume les ~56 ajouts.

RISQUES & MÉTÉO : vigilance-meteo (param 101 dépts, LA fusion pilote), vigicrues-departement (param 85 dépts ~252 tronçons), vigieau (param commune INSEE — restrictions d'usage), risque-secheresse (param dépt — arrêté préfectoral, API VigiEau/RegLeau, Propluvia est MORT ; distinction avec vigieau inscrite dans les 2 descriptions), seismes-france + seismes-departement (param ~80 km) + seisme-mondial-majeur (USGS M≥7.5), indice-uv (param 101, Open-Meteo mutualisé), epidemies-france (Sentinelles), tempete-solaire (NOAA G≥4/R≥3), asteroide-frole-terre (JPL ≤1 LD + h≤24), essai-sirenes (1er mercredi midi), meteo-quebec/belgique/suisse/europe (params ; europe = 8 pays expat MeteoAlarm), pannes-hydro-quebec, cyclones-outremer (param 6 territoires VIGI971/972/973/974/976/978 — flux ZIP DPVigilance /vigilanceom/flux/dernier, clé METEOFRANCE_API_KEY commune métropole+OM, adm-zip, seuil orange, bulletins texte ignorés pour la décision ; ACTIVÉE 25/07/2026 ; Polynésie/NC écartées = endpoints JSON séparés /polynesie/ /ncaledonie/, extension candidate future). DORMANTES (clés) : risque-avalanche (MF DPBRA). ⚠️ meteo-forets N'EST PLUS DORMANTE (29/07) : la clé MF s'est révélée inutile, source publique CSV sans clé + enum ouvert aux 96 dépts + enabled=true — cf. fil « veilles paramétrées » (SQL dans init.sql, migrate à faire).

ÉNERGIE & CONSO : EcoWatt, Ecogaz, carburant (param 5 types), tempo (repli communautaire verified — RTE portail à souscrire avant nov), energie-tarifs, taux-livret-a, rappel-conso (param catégories), leboncoin-livraison (ALERTE RÉELLE depuis le 06/09/2026 : détecteur Dealabs unique via server/sources/lib/dealabs-promo.js — thread actif isExpired=false matchant leboncoin+mondial relay+0,99 ; since=publishedAt du thread (anti-respam ; le thread peut précéder la promo de plusieurs heures, jusqu'à ~39 h observé) ; until=null (Dealabs ne structure pas la fin) ; requires_confirmation=true conservé (second avis face à un signal communautaire) ; cycle global 30 min, cron dédié 3 min SUPPRIMÉ (promo = 67 h vendredi 14h → lundi 8h59, constaté sur page officielle /service/bons-plans le 06/09) ; scraping direct Leboncoin RETIRÉ du chemin actif, conservé en bloc commenté daté (DataDome intermittent ~10 requêtes puis blocage ~30 min, motif reproduit 31/07 et 04/09) ; attribution « via Dealabs » dans la description), ce-qui-change (mensuel service-public.gouv.fr), echeances-fiscales (TF/THRS/PAS 1er sept/remboursement 24-31 juil).

ÉCONOMIE (nouveau, INSEE SDMX sans clé via lib/insee-bdm.js — publications définitives OBS_QUAL=DEF only) : indice-reference-loyers (trimestriel), inflation-insee (mensuel), chomage-stats (trimestriel). Écartés : taux-immobilier (BdF Webstat = compte requis), delais-titres (ANTS = SPA sans API).

TECH & DEV : ~38 statuts (gisement épuisé, FR/EU rarement Statuspage — opérateurs télécom vérifiés : rien de structuré), CERT-FR, node-lts, fin-de-vie-logicielle (param endoflife.date), maj-navigateurs, rdv-tech (Ubuntu 26.10 : 15 oct), github/npm/pypi/crates/packagist/rubygems-release (params ; release-factory pour les 3 derniers — migration npm/pypi dessus = lot audit), steam-jeu-promo (param appid), veille-hackernews (param mot-clé), veille-rss (param URL, SSRF-safe, préfigure V3), prix-turing (fenêtre mars-avril).

FRANCOPHONIE & MONDE : fetes-nationales (param 23 pays — Algérie 5 juillet acté), feries-quebec/belgique/suisse, jours-feries (param 13 zones), grands-anniversaires (curée : Monet 5 déc 26, Lindbergh 21 mai 27, Metropolis...), francophonie (20 mars), grandes-journees-mondiales (5 ONU), don-organes (22 juin, française), taux-de-change (param devises BCE/Frankfurter).

CULTURE & SORTIES : prix-litteraires, rentree-litteraire, festival-bd-angouleme, festival-livre-paris (16-18 avr 27), festivals-musique (Hellfest 17-20 juin 27, Rock en Seine 26-30 août 26), japan-expo (8-11 juil 27), fete-des-lumieres, spectacles-recompenses, ceremonies, grands-festivals, guide-michelin, grands-prix-gastronomie (50 Best 4 nov 26 Lima, Bocuse d'Or 24-25 janv 27), nuits-de-la-lecture, fashion-week (FHCM 3 éditions), rendez-vous-aux-jardins (4-6 juin 27), sorties-jeux/cinema-majeures (GTA VI 19 nov 26, Avengers Doomsday 16 déc 26), rdv-gaming (+Next Fest 19-26 oct), billetterie-concerts (config vide, curation au fil), loto-patrimoine (TODO), carnavals, saint-nicolas, semaine-du-gout, braderie-lille (5-6 sept 26), grands-salons, nobel-prix (5-12 oct 26), fete-science (2-12 oct 26).

VIE PRATIQUE & SOCIAL : vacances-scolaires (param zones), rentree-scolaire (1er sept 26), dates-bac (TODO BO), allocation-rentree-scolaire (19 août 26), prime-noel (16 déc), smic-revalorisation, cheque-energie, treve-hivernale, hausses-tarifs, loi-montagne, bison-fute, sncf-perturbations (attend clé), ouverture-ventes-sncf (config vide), grandes-causes (Octobre Rose/Movember/Téléthon 4-5 déc + TODO Pièces Jaunes/Restos), mercato-foot (LFP 1er sept/1er fév), grands-rendez-vous-sportifs, courses-mythiques (config vide TODO sept), semaine-bleue (5-11 oct 26), tour-de-france-passage (param dépt, config vide TODO oct — parcours par dépt non publiable fiablement), fetes-familiales/gourmandes, fetes par tradition, changement-heure, soldes, journees-patrimoine (19-20 sept 26), elections-france, saints-de-glace, ouverture-peche, jour-depassement (30 juil 26), rdv-planete, vendredi-13, premier-avril, journees-geek, beaujolais, black-friday.

ASTRO : eclipse-solaire (12/08/2026 — dans 3 semaines !), perseides, geminides, nuits-des-etoiles (7-9 août), evenements-astro (+éclipse Lune partielle 28/08/26 — AUCUNE totale visible de France 2026-27), grandes-marees (13-15/08, 11-13/09, 27/10), aurores-france, lancement-spatial, iss-passages (param 15 villes+Gap — EXPÉRIMENTALE : iss-api.fly.dev communautaire sans SLA, marquée FRAGILE, repli N2YO prêt-à-brancher).

PÉPITES VIRALES : fete-des-prenoms (param prénom, 326 prénoms + 48 variantes, table vérifiée — bug de dates détecté par tests et corrigé), rappel-personnalise (param "JJ/MM Libellé" récurrent — première brique V3 datée EN PROD).

⚠️ FRAGILES (API non officielles → surveillance Robot 1) : meteo-suisse, pannes-hydro-quebec, iss-passages.

⚠️ CARTES PANNEAUPOCKET CURÉES (vague L, 18 cartes broadcast display_order 443-460 + 441 canal-de-gap) = dépendantes de la vitalité de tiers (une entité PanneauPocket peut cesser de publier). Contrôle Robot 1 à prévoir : carte sans panneau depuis 90 j → candidate à désactivation (RAPPORT uniquement, jamais de désactivation automatique). Rappel prospection : la famille gendarmerie est massivement dormante (~85 %), les cartes retenues ont été choisies parmi les brigades actives — donc à re-vérifier en priorité.

ÉCARTÉES documentées (= liste de courses si un flux apparaît) : alerte-enlèvement (RSS officiel 404, diffusion médias/FR-Alert only), grèves, ZFE (légalement instable — seuls Paris/Lyon certains), séries (agrégateurs only), OMS/USPPI, INES nucléaire, Téléray, Reddit (OAuth requis), don-du-sang (ni bulk ni INSEE — API Carto EFS searchnearpoint lat/lon non contractuelle = seule voie), moustique-tigre, foire-aux-vins, nuit-des-chercheurs 2026 (pas de financement UE), MaPrimeRénov (ouvertures réactives au budget, pas de calendrier), sargasses, conseils-voyageurs, stations 05, nappes, baignade, jackpot, taux-immobilier, delais-titres, statuts opérateurs télécom, saisons-astronomiques (doublon fetes-laiques). NOUVEAU (cycle vagues) : veille-artiste-spotify (Developer Mode Spotify durci fév. 2026 → remplacée par Deezer ; fichier supprimé, ligne DB orpheline PURGÉE le 26/07), sfr-box (doublon SHA-256 sfr-red-mobile), WHO DON (RSS 404), Marie Curie / Four Colors / Dolly (pas de jalon rond dans la fenêtre), streaming phase 2 Spotify/Disney+/… (SPA anti-bot → headless requis), cyclones Polynésie/Nouvelle-Calédonie (endpoints JSON séparés /polynesie/ /ncaledonie/, hors flux vigilanceom — extension candidate de cyclones-outremer). NOUVEAU (prospection 29/07, cf. fil « veilles paramétrées ») : AlerteCyber/cybermalveillance (doublon de cert-fr-alertes avec 3-8 j de retard, aucun flux structuré), Pollinariums Sentinelles/alertepollens.org (alertes e-mail aux inscrits, aucun état public), ~~alerte-enlèvement~~ CONFIRMÉ ÉCARTÉ (déjà listé ci-dessus ; re-vérifié 29/07 : site mono-page sans aucun état « alerte en cours », et diffusion massive TV/radio/FR-Alert par construction → une carte opt-in serait plus lente et moins couvrante).

VAGUES 2026 (21-24 juil, ~56 sources ajoutées/refondues, display_order ≥ 384 — détail exhaustif : rapports/synthese-etat-alertes-2026-07-23.md). Familles :
- GÉO fine : qualite-air + pollens (Atmo France, attribution visible), vagues-submersion, veille-hydrometrie (station+seuil), ours-pyrenees (P dépt) ; COMMUNE (résolveur nom→INSEE/coords) : eau-potable-commune, catnat-commune, traditions-locales, marathons-villes, iss-passages (commune libre via coords, ex-enum 16 villes).
- VEILLE D'ÉTAT IMPRÉVISIBLE (cœur de cible, anti-rétroactif systématique) : veille-page, veille-stock, veille-entreprise, veille-boamp (marchés publics), crypto-seuil (paire+seuil), veille-emploi (France Travail), veille-twitch (Helix), veille-legifrance (JO), veille-arxiv, domaine-disponibilite, domaine-securite (URLhaus), hausse-tarif-operateur (hash-diff PDF FAI), hausse-tarif-streaming (Netflix/Deezer).
- SCIENCE : eruption-volcanique (GVP), exoplanete-habitable (NASA TAP), retraction-article (Crossref/Retraction Watch), ondes-gravitationnelles (GraceDB, + mécanisme de CORRECTION/rétractation), fete-science maj, grands-anniversaires +3 jalons.
- CONSO/CULTURE/ADMIN/OM : IPC alim, immo ancien, actus service-public, UFC-Que Choisir, ANSM, veille-artiste-deezer (remplace Spotify), grands-concerts, fashion-week, conventions-manga, actualisation/recensement/CFE/bourses/Crous/Parcoursup/CAF, commémorations + soldes + tours cyclistes outre-mer.

PANNEAUPOCKET (nouveau, 3 cartes en prod, orders 440-442). Contexte : app.panneaupocket.com = 15 600+ collectivités, pages /ville/ SERVER-RENDERED (parse HTML, tous les panneaux sans JS), PAS de robots.txt (404), PAS d'API de lecture publique documentée HORMIS GET /public-api/city/FR-<code dépt INSEE> (JSON sans auth, ~11 Ko/dépt, champs id/name/zipCode/url ; FR-2A/2B + FR-971..976 OK, PAS les codes ISO région ; liste nationale /public-api/city 5,8 Mo NON utilisée). Les 3 cartes : panneaupocket (P, 440, ['vie-locale','local','mairie']) — veille des panneaux d'UNE collectivité par URL de page /ville/ (power-user) : nouveau + modifié + annulé, agrégé si multiple, TTL 2h, attribution « via PanneauPocket ». arrosage-canal-gap (B, 441, ['vie-locale','eau']) — ASA du Canal de Gap, FILTRE THÉMATIQUE irrigation (arrosage, tour(s) d'eau, restriction, autorisation, remise en eau, coupure, irrigation, ressource en eau), mention canaldegap.fr ; panneaux hors-thème gardés en référence id→hash mais JAMAIS alertés (un hors-thème modifié pour le devenir déclenche proprement). ma-collectivite (P, 442, ['vie-locale']) — entrée VILLE (jamais d'URL), lookup → désambiguïsation par LISTE (mairie/ASA/syndicat/gendarmerie — PanneauPocket très utilisé par les gendarmeries), multiple:true (mairie + ASA = 2 instances). POSITIONNEMENT : complémentarité assumée avec vigieau/risque-secheresse (restrictions PRÉFECTORALES, périmètre national) vs cartes PanneauPocket (restrictions RÉSEAU d'irrigation + vie locale, hyper-local) — deux mondes distincts, aucune API nationale pour le second.
VAGUE L (18 cartes BROADCAST curées, display_order 443-460) : cartes thématiques pré-remplies par-dessus le même moteur, une collectivité en dur chacune, via helper makeCurated (presets de filtre par mots-clés normalisés, ~8 lignes/carte). Composition : 6 eau (regie-metz, provence-verte, isle-dronne, charles-chaigneau, puisaye-forterre, coteaux-lizon), 3 déchets (saulieu, la-saucelle, campagne-caux), 3 gendarmerie (securite- albi/bayeux/essarts), 4 infos locales (chablis, agly-fenouilledes, buech-devoluy, chabris-bazelle), agenda-luc-en-diois, cantine-a2m2v. arrosage-canal-gap (441) = la 1re du genre. PROSPECTION REJOUABLE : sélection via l'endpoint /public-api/city (par dépt) + mesure d'activité (une entité doit publier régulièrement) ; la famille gendarmerie est massivement dormante (~85 %), les retenues sont des brigades actives. OBSERVATION D'USAGE (dette légère) : les presets de filtre sont à ajuster si spam constaté — cas connus de SUR-INCLUSION assumée v1 : eau-charles-chaigneau (factures captées par « réseau/usage »), eau-coteaux-lizon (« coupure de courant » capté par « coupure »). À affiner à l'usage réel, pas bloquant.

## OpenAlert v2 (acquis, chantier terminé)

params enum|string|number, un param plat, multiple ; rétrocompatible (sans params = broadcast v1). DB : subscriptions.params + index unique d'expression, source_param_states (état par combinaison — SEULES LES COMBINAISONS SOUSCRITES sont calculées, rien d'autre n'existe en base), sources.params_schema, subscriptions.muted (pause sans désabonnement, filtré aux destinataires only). Poller : checkWithParams(combinaisons souscrites), transitions factorisées (decideTransition pure), libellés résolus partout, épisodes since≥24h. UI générique pilotée par params_schema (chips + « + ajouter » cyclique — exclusion mutuelle param-add/param-form corrigée par .param-add[hidden]). Pages statut : onglets d'instances + sélecteur + ?param=X. Fusions à zéro perte (pattern : migration idempotente + report d'état/since + enabled=false réversible + 301). Polling externe (safe-fetch SSRF, EXTERNAL_MAX_COMBOS=20, allSettled, round-robin). DoomName externe paramétrée (www.doomname.com/alert.json — apex doomname.com cassé côté Railway Domains, www suffit).

Acquis d'architecture cycle vagues (réutilisables) : (1) FRONT MULTI-CHAMPS — cards.js/site.js rendent un contrôle par descripteur params_schema (fields.map) et collectent tout au submit, rétrocompatible 1-champ ; débloque seuil+cible (crypto-seuil, veille-hydrometrie). Types : enum|string|number|commune|commune-coords. (2) RÉSOLVEUR COMMUNE — server/sources/lib/commune-insee.js : nom→INSEE (désambiguïsation dépt) et nom→{lat,lon}, résolu À LA SOUSCRIPTION (jamais au poll) via geo.api.gouv.fr, caches mémoire permanents ; branché dans routes/myalerts.js (toggle-param). (3) PATTERN VEILLE D'ÉTAT IMPRÉVISIBLE + anti-rétroactif : id stable par item, 1er cycle = amorçage SANS alerte, seul un id jamais-vu déclenche, échec → inactive (jamais de faux positif). (4) OAUTH2 client_credentials factorisé (rte/francetravail/twitch/legifrance-auth.js — token cache mémoire, renew -5min ; corps vs en-tête). (5) HASH-DIFF lib/hash-diff-html.js + hash-diff-pdf.js (denoise puis hash, on ne lit jamais le contenu). (6) ATTRIBUTION licence VISIBLE dans le message de notif (Atmo France/AASQA), pas qu'en commentaire. (7) LIB PANNEAUPOCKET — lib/panneaupocket-parser.js (fetch+parse d'une page /ville/ : id public ?panneau=, titre, texte, annulé, PDF joint ; validPanneauUrl : https + hôte exact + /ville/) + lib/panneaupocket-veille.js (FACTORY createParamSource/createBroadcastSource ; anti-rétroactif id→hash, hash SANS denoise = fidélité aux créneaux horaires ; prédicat `alertable` optionnel = filtre thématique ; re-validation validPanneauUrl AVANT fetch). Consommée par panneaupocket, arrosage-canal-gap, ma-collectivite (comportement strictement identique, tests inchangés). (8) TYPE DE CHAMP FRONT dynamic-enum = COMBOBOX autocomplete (cards.js paramControl + site.js), AUCUN code spécifique PanneauPocket au front : UN SEUL champ visible .dyn-search (role=combobox ARIA, aria-expanded/controls/activedescendant, aria-live) ; frappe debouncée 350ms → GET /api/param-lookup/:source (+&dept=<dépt profil> = biais désambiguïsation générique) → .dyn-listbox d'options (role=listbox/option), navigation clavier flèches/Entrée/Échap + clic ; SÉLECTION = libellé affiché dans le champ, URL posée dans input HIDDEN .dyn-value (.param-input data-key INCHANGÉ → readParam/contrat serveur intacts), abonnement déclenché (parité non-géo : commit = abonnement immédiat) ; RE-FRAPPE après sélection = INVALIDATION (hidden vidé, retour recherche). Pré-remplissage profil : LBADefaults.ville amorce la RECHERCHE (jamais pré-sélection), input synthétique au montage. Route /api/param-lookup/:source (myalerts.js, loader calqué le-point, dispatch vers export lookup(q,dept) du module, dept validé isValidDepartement, 404 si inconnue/non-enabled/sans lookup, q borné ≤60, throttle 30/min) ; params.js type dynamic-enum (validé comme string + pattern ; pattern RETIRÉ des schémas soumis = anti-ReDoS). lookup(q,dept) résout via geo.api.gouv.fr (nom→INSEE+CP, &codeDepartement si dépt) puis /public-api/city/FR-<dépt> (cache 24h/dépt). LIBELLÉS partout via resolveLabel : branche dynamic-enum dérive un libellé lisible du slug de l'URL (…/398423648-asa-du-canal-de-gap-05000 → « ASA du Canal de Gap (05000) »), même pattern que commune→nom (valeur canonique in, libellé humain out, SANS stockage) → chips/mes-alertes/statut/emails, jamais l'URL brute.
PERSISTANCE DES RÉFÉRENCES ANTI-RÉTROACTIVES (Lot 0+1, VALIDÉ EN PROD 25/07/2026) : la dette « cache mémoire perdu au redéploiement » est LEVÉE pour les sources équipées. Colonnes ref JSONB sur source_states + source_param_states (ALTER idempotent). Contrat OPT-IN par source : loadRef(params?, data) hydrate la référence AVANT le check ; dumpRef(params?) renvoie la structure sérialisable, ou undefined si INCHANGÉE (aucune écriture). Câblé dans poller.js (processSource/processParamSource) SANS toucher decideTransition/applyResult (machine à états prouvée inchangée). Écriture ref INDÉPENDANTE des transitions (upsert même en still-inactive → la ligne existe dès l'amorçage). Dégradation silencieuse TOTALE (colonne absente / erreur DB → log discret + comportement mémoire-seule, jamais de crash ni de fausse alerte). Garde-fous : 64 Ko côté dumpRef (au-delà : non persisté), 256 Ko côté poller (ceinture) ; Sets non bornés = CAP GLISSANT FIFO (1000/Set pour ondes). PROD 25/07 : 20 broadcasts + 7 combos peuplés (4 pilotes panneaupocket/ma-collectivite/arrosage-canal-gap/ondes-gravitationnelles + les 18 cartes vague L), formes conformes (array [id,hash] ; object {seenConfirmed,alerted,corrected}). Un redéploiement ne crée PLUS de trou pour ces sources. LOTS RESTANTS (mécanisme prouvé, ouvrir au prochain fil) : Lot 2 scalaires (veille-page, veille-stock, veille-entreprise, veille-boamp, veille-twitch, hausse-tarif-operateur/streaming, eau-potable-commune, catnat-commune, crypto-seuil) ; Lot 3 Sets cappés (veille-emploi, veille-arxiv, veille-legifrance, retraction-article). EXCLUS : domaine-disponibilite/domaine-securite (anti-rétroactif déjà porté par la machine à états du poller).
SURVEILLANCE PanneauPocket : (a) stabilité des ids ?panneau= à l'ÉDITION d'un panneau — si régénérés, une modif apparaît comme « nouveau » → basculer la dédup sur un id interne (consigné aussi en tête de ma-collectivite.js ; candidat liste Robot 1) ; (b) panneau « image seule » = hash sur titre seul, remplacement d'image NON détecté (jamais de faux positif) ; (c) TODO v1.1 lookup : désambiguïsation homonymes (« Saint-Denis 93 »), limite actuelle = résolution au plus peuplé (Saint-Denis→974). AUCUN test versionné dans le repo (les « tests » des rapports = scripts jetables) — filet de régression inexistant pour Robot 1/2 ; chantier candidat : npm test minimal node:test (parser panneaupocket, resolveLabel, decideTransition, validations d'URL).

## Fonctionnalités produit

- Heures de veille : 23h-8h Paris défaut, différé + digest (deferred_notifications), tzdata vérifié. Limite : fuseau global — à adapter pour Québec/expat.
- Personnalisation : country/departement/region/ville/interests → boost tri + reco (reco jamais hors-département, jamais de source disabled). region/ville ajoutés (mêmes garde-fous que departement, visibles/éditables dans Mon compte, *_source 'auto'/'manual').
- Auto-remplissage 1re connexion : display_name (Google given_name / GitHub name / email → collision Hugo2..8) ; pays par geoip-lite local ; departement/region/ville par IPLocate (voir section « Géoloc IP »). clientIp() : cf-connecting-ip (Cloudflare, vérifié par ORIGIN_SECRET) en priorité, sinon x-forwarded-for premier IP PUBLIC (Railway CGNAT 100.64/10). RGPD en confidentialité.
- Pré-remplissage géo sur cartes paramétrées : pour un schéma clé departement/region/pays/ville, le contrôle (select/input) est pré-rempli depuis le profil (pas de chip séparée). GÉO : switch « S'abonner » visible mais OFF, activation UNIQUEMENT au clic (jamais au pré-remplissage). NON-GÉO : switch visible mais disabled tant que vide, activation immédiate au choix/saisie (comportement d'origine). Une fois abonné, interrupteur pause/reprise (toggle-mute) = pause sans perdre le paramètre (distinct de la suppression d'instance). Seul le département matche une source aujourd'hui (region=Québec only, pays=slugs diaspora, ville=slugs iss/INSEE) → prefill générique, rien de faux affiché. ⚠️ CORRIGÉ 30/07 (cf. fil « veilles paramétrées ») : sur une carte à 2+ champs, un second champ FACULTATIF resté sur son placeholder faisait échouer SILENCIEUSEMENT tout l'abonnement — readParam() refusait le formulaire entier, y compris le champ géo pourtant correctement pré-rempli. Un champ `required:false` est désormais pré-sélectionné sur son défaut et marqué `data-optional` ; vide, il est OMIS au lieu d'invalider (le serveur applique son défaut). Les enums REQUIS à défaut (carburant, rappel-conso, jours-feries, fin-de-vie-logicielle) restent inchangés sur « Choisir… » — un champ requis doit rester un choix explicite.
- Likes + Favoris : table favorites + /favoris + sync montante localStorage→serveur + cœur header. Limite : sync descendante multi-appareils absente.
- « Les plus populaires » (ex-Sélection, top-12 likes public, slug interne 'selection' conservé) + « Nouveautés ».
- Collections : 7 packs officiels (étagère carrousel infini + fondu bords, adoption 1 clic idempotente, params résolus profil>pack>à compléter, sources dormantes auto-incluses à l'activation) + Decks utilisateurs (15 max — relevé de 10 au fil cartes & decks, compteur « X / 15 » visible + bouton grisé au plafond ; 24 motifs SVG + 11 teintes — UN SEUL CSS partagé toutes pages, partage lien non-listé token 128 bits révocable, fork-copie avec attribution figée, pseudo public display_name unique « Mon pseudo », signalements 3 IP → suspension auto, remontée Robot 1 ; tuiles PLEINE TAILLE LBADeckStack depuis le fil cartes & decks — voir section dédiée). « Ma collection » (abonnements) ≠ « Mes decks » (compositions). Avatar initiale. Depuis le fil 27/07 : catégories auto-calculées + filtrage grille des tuiles-deck, visibilité unifiée public/private/official, compteur d'adoptions (collection_adoptions) ; système de POINTS COSMÉTIQUES + BOUTIQUE de skins (/boutique) — détail dans la section « Fil 27/07 ».
- Vue kiosque (fil 27/07) : bascule CARTES ⇄ LISTE DENSE (préférence de compte subscribers.view_mode, localStorage pour l'anonyme), contrôle synchronisé toolbar ↔ Mon compte › Apparence ; la vue liste est une projection des cartes (zéro logique dupliquée). Détail dans « Fil 27/07 ».
- Soutien financier (EN PROD, actif) : dons PayPal + Ko-fi. Archi = endpoint GET /api/support-links (lit PAYPAL_DONATE_URL / KOFI_URL côté serveur, ne renvoie l'URL que si posée ET https://) + public/js/support.js (rendu conditionnel : bouton actif <a target=_blank rel=noopener> si variable posée, sinon <button disabled> « bientôt » ; activation INDÉPENDANTE par service). Posées sur Railway : paypal.me/labonnealerteFR + ko-fi.com/labonnealerte. 3 emplacements synchronisés (soutenir.html + index.html ×2, data-label). Libellés : « Me soutenir avec PayPal » / « ☕ M'offrir un café » (icône café SVG inline, vapeur animée CSS pure dans site.css .sup-coffee, prefers-reduced-motion respecté). Widget externe Ko-fi REFUSÉ (dépendance tierce + rupture de charte). Mention « arrivent bientôt » retirée des textes.
- Le Point (/le-point) : « en ce moment » (broadcast actifs + agrégats paramétrés des combinaisons souscrites — assumé dans le libellé) + « à venir » 10 j (contrat upcoming() de calendar-factory, 72 sources ; paramétrées exclues — CHANTIER VALIDÉ EN ATTENTE : upcoming() toutes-combinaisons pour jours-feries + fetes-nationales). Cache 2 min, état calme assumé, pending exclus. Vitrine de lancement désignée.

## Audit & dette (audit-architecture.md à la racine, aucun 🔴 sécurité)

Lot 1 FAIT (index subscriptions(source_id) + partiel source_param_states actifs + import mort retiré — candidats favorites/deferred/reports écartés car déjà couverts). Lots 2-5 restants : migration npm/pypi sur release-factory, belgique/suisse sur lib/meteoalarm, découpe site.js (~1800 l.)/site.css (~2000 l.)/init.sql (~2500 l.), enum dépt dédupliqué, rate-limit /auth/*, 11 globals LBA*. À préserver : factories, contrat v2, idempotence, anti-spam.
DB vs CODE (vérifié 24/07, fil PanneauPocket) : écart du 23/07 RÉSOLU — migrate.js passé, sources display_order 433-439 (Deezer→ondes gravitationnelles) en base et enabled. Orphelin veille-artiste-spotify : ABSENT d'init.sql ; init.sql ET migrate.js ne contiennent AUCUN DELETE (migrate = DROP promo_status + exécution d'init.sql, rien d'autre) → la purge (DELETE manuel) a été FAITE le 26/07 via scripts/purge-veille-artiste-spotify.js (source_states + sources, transaction + garde-fous : 0 abonnement/point/instance) → DB alignée sur init.sql. Compteurs init.sql (comptés par parse, 25/07) : 288 lignes sources, 264 enabled (24 désactivées : UPDATE ciblés + fusions en masse vigilance-meteo-%/vacances-zone-*), dont les 3 cartes PanneauPocket socle (440-442) + 18 cartes curées vague L (443-460) + cyclones-outremer (activée) ; orphelin spotify purgé le 26/07 (DB = 288 lignes sources, alignée sur init.sql). Paramétrées PanneauPocket : panneaupocket + ma-collectivite (arrosage-canal-gap + les 18 curées = broadcast). MIGRATIONS 25/07 (idempotentes) : ALTER TABLE source_states/source_param_states ADD COLUMN ref JSONB (persistance anti-rétroactive, cf. OpenAlert) — vérifiées peuplées en prod (information_schema + pg_column_size, lecture seule).

INTÉGRITÉ LIKES — **CODE LIVRÉ (14/08/2026), MIGRATION ET BACKFILL EN ATTENTE D'EXÉCUTION PAR HUGO**, avec DEUX SUITES OUVERTES. `likes_count` était **strictement croissant** (incrémenté sans dédup serveur ; la seule barrière était `localStorage`, contournable par un vidage de cache, un autre appareil ou un `curl`). CLARIFICATION CLÉ DU CHANTIER, à garder en tête : **« favori » et « like » sont DEUX concepts distincts qui partagent le même bouton** — le favori (table `favorites`) est privé et dédupliqué, le like (`likes_count`) est public et ne l'était pas. Table `source_likes` (PK composite source_id+subscriber_id, patron `favorites`/`community_report_spots`) : `sources.likes_count` n'est plus incrémenté mais RECALCULÉ depuis elle, dans la MÊME transaction (`applyLike()` de routes/api.js). Le like exige désormais un COMPTE (un anonyme n'a pas d'identité à dédupliquer) : POST/DELETE `/api/sources/:id/like` répondent 401, et le ❤ anonyme (classe `.like-anon`) mène à /connexion. `likeLimiter` 20/min/IP conservé en ceinture. Les decks n'ont JAMAIS eu de compteur propre (`total_likes` = SUM des cartes, calculé à la volée) → rien à faire de leur côté. Historique NON reconstituable (personne n'a jamais enregistré qui aimait quoi) → remise à zéro franche des 14 likes, sauvegardés dans `rapports/sauvegarde-likes-<horodatage>.json` (avec le SQL de restauration) par `scripts/reset-likes.js` (`--check` puis `--apply`, refuse de tourner si `source_likes` est déjà peuplée). SUITES : (1) ⚠️ RÉGRESSION ASSUMÉE — un anonyme ne peut plus poser de favori local via le ❤, donc le filtre « Mes favoris » du kiosque est de fait réservé aux connectés (la clé localStorage `lba-likes` est CONSERVÉE : elle sert encore au marquage des cœurs, à /api/favorites/sync et au filtre — ne pas la purger). À rouvrir si l'on veut rendre les favoris anonymes au ❤ sans lui redonner le vote. (2) CHANTIER DISTINCT NON OUVERT : rebrancher le dé-like sur l'interface — aujourd'hui le ❤ du kiosque retire le FAVORI (`DELETE /api/favorites`) et ne touche pas au like ; `DELETE /api/sources/:id/like` existe, est correct et testé, mais n'est appelé par aucun écran. Découplage favori/like VOLONTAIREMENT conservé (décision Hugo 14/08). MÉTHODE DE TEST : routes réelles jouées contre la VRAIE base dans une transaction **intégralement ROLLBACK** (aucune écriture de test en prod) — technique réutilisée depuis pour d'autres chantiers, avec sa limite désormais connue (cf. fil « généralisation des cartes communautaires » : un effet de bord qui rouvre sa propre connexion échappe au rollback). ⚠️ **SÉQUENCE À EXÉCUTER PAR HUGO, DANS CET ORDRE** : `git push` → `railway up` → `node server/db/migrate.js` → `node scripts/reset-likes.js --check` → `node scripts/reset-likes.js --apply`. Le script **refuse `--apply` tant que `source_likes` n'existe pas** : la migration doit précéder, sans exception.

FIX ISOLÉ EN ATTENTE (constaté 27/07, chantier rappel-conso) : la liste `GRAVE` de server/sources/rappel-conso.js ne couvre PAS les motifs `asphyxie` ni `dommage à la vue`, présents en clair dans les `risques_encourus` de l'API (constatés sur des fiches réelles de la catégorie « automobiles et moyens de déplacement »). Une fiche ne portant QUE ces motifs passe sous le radar. Concerne les 10 catégories, pas seulement l'automobile → petit fix global sur la liste GRAVE, testable indépendamment, à faire dans un commit dédié. Non traité au fil de l'eau (décision Hugo 27/07).

EXTENSION EN ATTENTE DE DEMANDE (27/07, chantier veille-agenda) : le parseur server/sources/lib/ics-parser.js couvre FREQ=DAILY/WEEKLY/MONTHLY/YEARLY + INTERVAL/COUNT/UNTIL/BYDAY(WEEKLY)/EXDATE/RECURRENCE-ID. Tout le reste (BYMONTHDAY, BYSETPOS, BYMONTH, BYDAY préfixé « 2MO », BYDAY hors WEEKLY, FREQ horaires) fait IGNORER l'événement — règle assumée « jamais de fausse date d'occurrence » (mieux vaut manquer une alerte qu'en inventer une). Angle mort utilisateur connu : un agenda perso avec « le 3e vendredi du mois » ne déclenche rien (compté dans un console.warn par flux). PROCHAINE EXTENSION si un usager s'en plaint : BYMONTHDAY puis BYSETPOS (les deux plus rentables). Ne pas élargir sans demande réelle — décision Hugo 27/07.

PISTE REPORTÉE (prospection 29/07, vague « risques saisonniers ») — EAUX DE BAIGNADE : aucune source NATIONALE ouverte. Hub'Eau n'a pas d'API baignade (v1/v0/baignades → 404) ; data.gouv ne renvoie que des jeux LOCAUX hétérogènes (0 à portée nationale sur 45 résultats) ; baignades.sante.gouv.fr est un site legacy ISO-8859-1 sans API ni robots.txt. Les données SISE-Baignades du ministère ne sont PAS en open data. À ROUVRIR si le ministère publie SISE-Baignades, OU pour une carte VOLONTAIREMENT LOCALE façon PanneauPocket — points de départ identifiés : Brest métropole (« Points de contrôle des eaux de baignade ») et Haut-Léon Communauté, tous deux en licence lov2 et à jour (MAJ 28-29/07/2026). Ne pas retenter l'angle national sans signal nouveau — décision Hugo 29/07.

CHANTIER FRONT À OUVRIR UN JOUR — MULTI-PARAMÈTRES DANS L'UI (décision Hugo 29/07) : public/js/source.js ne rend que `params_schema[0]` ; tout paramètre au-delà du premier est invisible et réglable UNIQUEMENT par URL manuelle. Conséquences déjà subies : `preavis` de veille-agenda inatteignable (retombe sur 1 jour), et la granularité COMMUNE de pollens a été REPORTÉE pour cette raison (la vraie valeur du chantier pollens était le choix d'espèce, pas la finesse géographique — livrer 2 réglages invisibles n'avait pas de sens). À ROUVRIR ENSEMBLE le jour d'un vrai chantier front multi-paramètres : il débloquerait d'un coup `preavis` (veille-agenda) ET `commune` (pollens), plus les 3 sources multi-champs existantes (crypto-seuil, veille-emploi, veille-hydrometrie) qui vivent avec la même limite.

## Corrections UX récentes & accessibilité

Faits (fil précédent) : cartes mobile (.wrap media query), pref-chips pleine largeur, « Mon pseudo », bouton header min-width 185px, mes-decks CSS unifié, param-add[hidden].
Faits (ce fil) : hero padding petits smartphones (≤400px) ; page /source responsive (manifeste JSON scrollable, .src-head compact) ; croix .src-back collection remontée mobile ; header une ligne hors home (body.m-secondary) ; /le-point aligné (header standard injecté, h1 centré, labels de section « En ce moment »/« À venir » qui se chevauchaient en ≥1280px corrigés) ; menu mobile : ligne thème retirée ; Mon compte réorganisé — section « Apparence » (thème clair/sombre + « Renforcer les contrastes »), liens morts « Mes decks »/« Proposer une source » retirés de Divers.
✅ RÉSOLU — MENU MOBILE SCROLL : le flip Menu⇄Catégories était un preserve-3d (couche 3D composée → contenu figé au scroll). Converti en glissement 2D translateX (m-menu-flip sans preserve-3d, m-menu-face = conteneur scrollable overflow-y:auto). Le flip 3D des CARTES kiosque (.card-inner) est intact.
✅ RÉSOLU — PARTAGE /mes-decks : decks.js utilise window.LBAShare.openModal (grande modale unifiée collection/deck), plus de lien brut.
ACCESSIBILITÉ : body.high-contrast (toggle Mon compte › Apparence, persistance localStorage lba-contrast, appliqué site-wide via theme.js/site.js). ⚠️ La section « Apparence » a été refondue le 03/08 (thème à 3 états clair/auto/sombre) — voir le fil dédié. Règles CSS de contraste renforcé implémentées (overrides --muted/--line/--amber en light ET dark, ajustements par composant).
À vérifier post-déploiement : iss-passages en conditions réelles (un soir à Gap).
Pages de chargement : PAS de page unique — états locaux par page (.grid-loading home, .src-loading mes-decks/favoris/collection/deck), halo animé, reduced-motion respecté.

## Fil « Mon compte » — refonte mosaïque (03/08/2026)

PAGE ENTIÈREMENT REMISE À PLAT, périmètre strictement front (aucune table touchée ; une seule ligne serveur, cf. @pseudo).

MISE EN PAGE : la carte géante `.acct-card` (flip 3 faces) est SUPPRIMÉE. Titre `<h1 class="page-title" data-hero-done="1">` aligné sur les autres pages, puis une MOSAÏQUE de petites cartes `.acct-tile` — une par rubrique : Identité, Notifications, Personnaliser les alertes, Mon pseudo, Mes points, Heures de veille, Mes sources (conditionnelle), Nous soutenir, Apparence, Divers. Les `.notif-card` internes se fondent dans leur tuile (plus de carte-dans-la-carte).

CONSÉQUENCE ASSUMÉE DU DÉCOUPAGE — le flip 3D ne survivait pas à l'éclatement : historique et « Nous soutenir » deviennent deux VUES sœurs (`#acct-view-history`, `#acct-view-support`) commutées par `showAcctView()`, qui réutilise `swap()` (la transition déjà employée pour grille ⇄ panneau). CSS mort retiré (`.acct-front`, `.acct-back`, `.acct-support-face`, `.acct-face-support`). Le ✕ flottant est retiré (il se posait sur une tuile) : fermeture par « ← Revenir à ma collection », le header, le logo, ou toute interaction recherche/catégories.

MAÇONNERIE (`layoutAcctMosaic()`, site.js + `.is-masonry`) : les rubriques ont des hauteurs très inégales, une grille en rangées laissait un grand vide sous les tuiles courtes. La grille reste une VRAIE grille (auto-fill 340px), mais rangées de 4px + `grid-row-end: span N` calculé par tuile → chaque carte se cale sous la précédente de SA colonne. `ResizeObserver` par tuile (les rubriques se peuplent en asynchrone et changent de hauteur), + `resize`, + retour depuis une vue secondaire. Retiré sous 2 colonnes (mobile) ; sans JS la grille normale reste valide. **Tentative écartée : `columns` CSS** (rendait une colonne unique dans notre contexte). Écart vertical piloté par `--acct-row-gap` (50px desktop / 28px mobile) — variable OBLIGATOIRE car en maçonnerie le `row-gap` CSS est à 0 et l'écart est réinjecté dans le span.

profile.js — RENDU ÉCLATÉ en 3 montages (`#profile-mount`, `#pseudo-mount`, `#points-mount`) via un helper `paint(id, html)` avec repli sur `#profile-mount`. Même `state`, mêmes handlers (récupérés par id global), aucun second cycle de rendu.

@PSEUDO AFFICHÉ (seule modif serveur du fil) : `subscribers.pseudo` n'était pas exposé au client. Ajouté au SELECT et à la réponse de `GET /api/my-alerts` — **et relu après `applyAutofill`**, qui le pose en même temps que le display_name (sinon null au tout premier chargement). Rendu sous « Mon pseudo » en ligne « Mon identifiant public », lien `/u/:pseudo`. Libellé distinct du champ du dessus À DESSEIN : le handle est posé UNE fois et n'est jamais régénéré au renommage (server/pseudo.js).

THÈME À 3 ÉTATS (Apparence) : `light` / `dark` / **`auto` (défaut)**, sélecteur segmenté `.pref-seg` remplaçant l'interrupteur binaire. `auto` = AUCUNE valeur stockée — équivalence volontaire pour que les pages chargeant theme.js (qui ne reconnaît que light/dark et retombe sur le système) donnent le même résultat sans logique dupliquée. **Ce qui manquait vraiment** : le thème système n'était lu qu'au chargement — écouteur `matchMedia('(prefers-color-scheme: dark)')` ajouté dans site.js ET theme.js (repli `addListener` Safari < 14), qui ne réapplique QUE si la préférence est `auto`. API : `window.getThemePref()` / `window.setThemePref()`. `toggleTheme()` (bouton ◐ menu mobile) conservé et sort d'auto à dessein. Le segment peint la PRÉFÉRENCE, pas le thème résolu (sinon « Auto » jamais visible comme sélectionné).

AUTRES : compteur « Ma collection » CORRIGÉ — il comptait les abonnements (`data-subscribed`), il compte désormais les FAVORIS via `cardIsFav`, prédicat identique au filtre `?mode=favoris` ; rafraîchi à chaque action du cœur, rollbacks réseau inclus ; devenu un lien vers `/?mode=favoris`. « Sur cet appareil » (push) passe du bouton « Activer » à un `.notif-toggle`, comme « Par email ». `acct-delete-link` déplacé dans la rubrique Divers.

## Chantier cartes & decks (fil clôturé 25/07/2026)

FORMAT CARTE À JOUER (déployé) : ratio 5/7 sur toutes les cartes du site ; grille adaptative repeat(auto-fill, minmax(250px,1fr)) (remplace les 3 colonnes fixes) ; description en troncature + fondu (line-clamp) ; badge « vérifié » repositionné en haut-droite du titre (ne casse plus sur 2 lignes) ; contrôles de paramètre contraints en largeur (plus de débordement inter-cartes). RESTE : select « Choisir département… » tronqué illisible sur mobile très étroit ; coupure de mot disgracieuse sur titres longs (ex. « Restrictions ») en carte étroite — pistes hyphens:auto ou réduction de font-size cible ; vide central sur cartes courtes/non paramétrées, attendu comblable par le contenu plus riche des sources v2.

DECK-STACK (déployé) : étagère carrousel home DÉSACTIVÉE (commentée, PAS supprimée — public/index.html + public/js/site.js, bloc daté 2026-07-25, réactivable). Decks (collections + decks perso) = désormais des tuiles PLEINE TAILLE mélangées aux cartes classiques dans la grille du kiosque (et Mes decks, formulaire, détail). Composant unique LBADeckStack (public/js/deck-stack.js) = pile de jusqu'à 3 vraies cartes (recto complet, switch désactivé) + ruban teinté centré en forme de vague (dégradé arc-en-ciel reproduit pour tint-11). Carte de devant à l'échelle var(--ds-front) pour libérer la place latérale de l'éventail — écart de taille RÉSOLU 27/07 : carte de devant plafonnée à 300px via calc(300px / var(--ds-front)) (compense le scale) → parité visuelle avec les cartes classiques. Éventail : .ds-i1/.ds-i2 remplacent l'ancien scale(0.8) par un max-width identique au front + rotation/offset seuls pour l'effet de pile (évite le décalage vertical causé par scale + inset:0). dam-thumb (24px, menu « ajouter à un deck » au dos d'une carte) INCHANGÉ (ergonomie à repenser plus tard). deck-thumb (44px) SUPPRIMÉ, remplacé par le composant pleine taille partout, y compris en-têtes de détail. RUBAN + ÉVENTAIL CORRIGÉS 08/09 (CSS seul, site.css) : (a) le ruban n'est plus centré verticalement mais ancré par son BORD BAS via --ds-ribbon-bottom (56 % de la hauteur de pile par défaut), donc il grandit vers la fenêtre d'art et ne recouvre plus jamais .card-top ni .card-subtitle de la carte d'aperçu ; les 4 skins structurés, qui rangent titre et ligne de type ailleurs, surchargent la variable (École 24 %, Arcane 66 %, Assemblée et Dresseur 56 %). (b) l'éventail ne déborde plus sur la gouttière voisine : --ds-fan passe de 7 % à 4,5 % et le plafond de largeur des cartes d'aperçu (min(300px, 80 % de la face)) s'applique désormais À TOUTES les faces et dans TOUS les contextes, plus seulement dans .grid — la marge latérale nécessaire à l'offset + rotation est donc réservée par construction (pas d'overflow:hidden, la tuile reste en overflow:visible pour l'étiquette « Recommandée »). En compensation de ce plafond, la pile des en-têtes de détail (.deck-detail-layout .deck-card) passe de min(250px,48vw) à min(310px,88vw), sinon sa carte devenait trop étroite et son bloc de texte ne laissait plus de bande libre au ruban. Vérifié au navigateur (Playwright, 440 tuiles) sur /, /collection/:id, /u/:pseudo et un rendu reconstruit de la section « Skins de decks » de /boutique, en 1440 et 390 px, thèmes clair et sombre : 0 recouvrement, 0 débordement, ratio 2/3 et dégradé tint-11 intacts.

À TESTER / DÉCIDER (prochain fil « decks ») : (a) étagère carrousel — comparer engagement/découvrabilité vs mélange en grille, décider réactivation/ajustement/abandon définitif ; (b) catégories dédiées aux decks = FAIT 27/07 (collections.categories TEXT[], top-3 auto-calculé des cartes enabled, recalculé à chaque add/remove, jamais choisi par l'utilisateur = anti-abus ; backfill officiels + perso ; filtrage grille : tuile-deck visible seulement si data-cats matche le filtre actif hors « Toutes » ; Nouveautés/Populaires incluent désormais les decks via computeSpecialIds, caps 2/6 et 4/12). La logique « recommandé » (reco decks dans pickReco) = LIVRÉE au fil 30/07 (cf. section « Fil reco decks & verso deck ») ; (c) doublon de contenu ASSUMÉ : une carte peut apparaître seule ET en tête de pile d'un deck dans la même grille — à observer en usage réel ; (d) amplitude de l'éventail (--ds-fan) et échelle de la carte de devant (--ds-front) — réglages actuels à affiner après retour d'usage ; (e) feuilletage tactile (tap sur carte du fond = mise en avant) implémenté identique desktop/mobile — à limiter au desktop si la zone de tap s'avère trop étroite en usage réel sur petit écran ; (f) aperçu de deck en recto complet (« titre de la source » envisagé comme alternative plus légère, PAS retenu) — réévaluer si le rendu complet s'avère trop chargé ; (g) petites finitions CSS supplémentaires appliquées par Hugo après le dernier rapport (à documenter si notable).

NOUVEAUX SUJETS ACTÉS (prochain fil « decks », suite) : Communauté (forum intégré ?) ; présence réseaux sociaux ; alertes déclenchées par l'utilisateur (ex. « mon chien a disparu ») ; MODALE DE PARTAGE DECK à simplifier — suppression de la modale, boutons inline près du titre ; renommer « Gérer le partage » → « Partager » ; désactiver « Arrêter le partage » (fonction pas claire) ; visuel d'état vide pour « Ajouter des cartes » ; « S'abonner à ce deck » → « S'abonner » (NB : contredit volontairement l'acquis « PARTAGE = modale unifiée LBAShare » ci-dessus, qui décrit l'état ACTUEL) ; POSITIONNEMENT PanneauPocket/IntraMuros/Illiwap acté = complémentaire en façade, concurrent indirect assumé (même besoin, agrégation vs saisie manuelle mairie), OpenAlert-pour-mairies = suite logique du positionnement, piste V3 non prioritaire.

## Fil "decks — catégories, points, boutique & vue liste" (27/07/2026, déployé sauf mention)

DECKS — CATÉGORIES, FILTRAGE, RECO : cf. « Chantier cartes & decks » (b) mis à jour. En plus : collection_adoptions CRÉÉ (n'existait pas) — PK composite (subscriber_id, collection_id), alimente le compteur « populaire ». Visibilité deck UNIFIÉE : public/private/official, migration unlisted→public, kiosque servi par un seul endpoint /api/collections. Signalement (3 IP) : suspend le deck en private UNIQUEMENT, ne touche JAMAIS display_name du compte (fix appliqué après un premier essai fautif).

SYSTÈME DE POINTS COSMÉTIQUES (4 phases) — points de gamification, cosmétiques SEULEMENT (aucun avantage fonctionnel, cohérent ADN gratuit/anti-abus) :
- Phase 1 : points_ledger (append-only, anti-doublon par contrainte unique), subscribers.points_balance ; 4 événements (DECK_ADOPTED, ALERT_SUBSCRIBED, DECK_CREATED, DECK_ADOPTED_BY_OTHERS) ; helper points.js défensif.
- Phase 2 : rang PRIVÉ (jamais de classement public exposé — même prudence que /le-point) ; badge « Top 20 » (violet #a567e3, token à créer si réutilisé) sur decks publics ; opt-out RGPD RÉEL (exclusion du calcul, pas juste masquage) ; cache 2 min façon /le-point.
- Phase 3 : boutique /boutique, tables skins/user_skins, achat TRANSACTIONNEL (jamais de solde négatif) ; équipement au niveau dashboard ET par deck ; 4 skins placeholder d'origine (aurore/nuit-etoilee/menthe/or-royal, swatch plat, conservés). **9 VRAIS SKINS ajoutés depuis (cf. section « Fil skins de cartes ») : 5 teintes + 4 structurés, 100 % CSS, aucune image.**
- Complément : bouton « équiper un skin » directement depuis « Mes decks » (pas seulement la boutique).

VUE LISTE DENSE (nouvelle vue alternative) : COMPLÉMENT, pas remplacement du système de cartes — une ligne = une alerte sur desktop, objectif lisibilité/quantité (vs ludique/marketing). Exclut les tuiles-deck. Toggle PERSISTANT = préférence de COMPTE (subscribers.view_mode, 'cards'|'list', CHECK strict, DEFAULT 'cards') synchronisée entre appareils pour un connecté ; localStorage seul pour l'anonyme (aucun POST). Contrôle DOUBLE bidirectionnellement synchronisé : toolbar du kiosque ET Mon compte › Apparence. Accordéon GLOBAL (une seule ligne étendue à la fois) : clic ligne → description courte ; clic « i » → contenu page statut (uptime + timeline, remplace la description) ; clic « partager » → options inline (remplacent l'affichage). ZÉRO logique métier dupliquée : cœur/abonnement/partage/i FORWARDÉS aux mêmes handlers que les cartes (délégation DOM sur document, indépendante de la structure carte) — nouveaux modules public/js/view-mode.js (source de vérité data-view sur <html> + persistance) et public/js/list-view.js (rendu = projection des cartes de #grid qui restent l'unique source de vérité, sync() miroir), aucun second moteur de filtre (une seule source, site.js). Nouveau POST /api/my-alerts/view-mode dédié (auth). Modifiés : site.js (LBAKiosk, adoptAccount, render+sync), appearance.js (contrôle « Vue liste »), index.html (#view-toggle, #list, includes), site.css (~96 l.). Migration : subscribers.view_mode (ALTER idempotent + CHECK).

GRILLE & VISUEL : Kiosque .grid > .card { max-width:300px; margin-inline:auto } → 3 colonnes stables 1024→1920 (cause du bug initial : body flex-column + .wrap { margin:auto } désactive stretch → corrigé par width:100% sur main#alertes). /favoris : bug conteneur identique corrigé, plafonné à 4 colonnes fixes desktop (mobile inchangé). .decks-main : même fix width:100% appliqué préventivement. (Tuiles-deck 300px + éventail : cf. DECK-STACK.)

PAGINATION « afficher plus » — 3 régressions corrigées : (1) Trait blanc traversant l'écran = la carte-ancre « Proposer » (et tuiles-deck en fin de DOM) subissait un FLIP translateY plein écran → drapeau de contexte grow qui neutralise dx/dy pour les movers en pagination (PAS un seuil de distance, jugé trop fragile sur mobile). (2) Ordre cassé (nouvelles cartes insérées au milieu) = fallback de tuiles-deck en débordement inséré après le bloc caché au lieu d'avant → corrigé ; reco/filler connectés volontairement non touchés (dernier voulu). (3) Saut de scroll = scroll-anchoring navigateur → holdScroll épingle scrollY pendant la reveal, se libère au 1er vrai geste utilisateur (wheel/touch/clavier), PAS un seuil de distance (l'anchoring produit lui-même de gros déplacements).

DESCRIPTIONS COURTES/LONGUES : description (≤120, CHECK strict) + description_long (≤300, CHECK strict, nullable). 238 descriptions courtes réécrites éditorialement (ton accrocheur, zéro jargon) + 9 description_long re-rédigées proprement (anciennement tronquées brutalement à 300). PIÈGE méthodo (cf. Conventions) : un scan de fichier par motif texte peut rater des formes syntaxiques imprévues (UPDATE multi-colonnes où description n'est pas la 1re colonne du SET) — la vérif fiable = exécution réelle contre Postgres avec lecture du DETAIL: de l'erreur, jamais un grep/regex sur le fichier source.

DETTE source_states COMBLÉE : 41 sources (8 cassées + 33 dormantes) n'avaient jamais de seed source_states dans init.sql — fonctionnait en prod par création au runtime, aurait cassé une install neuve. Bloc « cohérence install neuve » ajouté, idempotent (INSERT ... SELECT ... WHERE NOT EXISTS).

BUG RÉSOLUTION GÉO iss-passages : resolveCommuneCoords (server/sources/lib/commune-insee.js) filtrait DUR par département du profil (codeDepartement en param de requête geo.api.gouv.fr) alors que l'ISS survole toute la planète → corrigé : département sert désormais au DÉPARTAGE d'homonymes, plus de filtre restrictif (dept = tie-breaker, pas filtre). resolveCommuneInsee (vigieau, local par nature) NON touché. Bonus : touche Entrée sur un champ paramètre ne validait rien (seul le debounce 700ms marchait) — ajoutée, cohérente quel que soit l'état d'abonnement (1er ou instance supplémentaire). (À vérifier en conditions réelles : cf. Corrections UX.)

SONDE D'OBSERVATION PROMO LEBONCOIN (phase SILENCIEUSE, aucune alerte envoyée) : source leboncoin-livraison bloquée par DataDome (IP datacenter Railway) sur scraping direct. Objectif produit central : détecter l'activation de la promo « livraison Mondial Relay 0,99€ » (aléatoire, week-end uniquement, aucune source officielle stable). 4 pistes évaluées, 1 retenue (Dealabs, communauté bons plans, zéro anti-bot rencontré) + 1 comparatif (retest Leboncoin-direct pour observer si le blocage DataDome varie dans le temps). Mode observation seule : promo_probe_log (nouvelle table append-only), check() renvoie TOUJOURS inactive() tant que la fiabilité n'est pas mesurée sur plusieurs vendredis. EXCEPTION ASSUMÉE à l'archi : cron dédié FIN */3 13-15 * * 5 (Europe/Paris) au lieu du cron global 30 min — documentée en commentaire « EXCEPTION ASSUMÉE, ne pas copier ailleurs ». Prochaine étape Hugo : consulter promo_probe_log après quelques vendredis pour comparer fiabilité/latence Dealabs vs Leboncoin-direct avant de brancher une vraie alerte. → CONCLUE le 06/09/2026 : 42/42 sondes Dealabs correctes (21 vrais négatifs 31/07, 21 vrais positifs 04/09 latence 2 s après début officiel), leboncoin-direct invalide (DataDome par vagues). Vraie alerte branchée (cf. ÉNERGIE & CONSO). Sonde reconfigurée en CONTRÔLE TEMPORAIRE : Dealabs seul, cadence 10 min, fenêtre vendredi 8h → lundi 9h59 (3 expressions cron), À DÉMONTER après 2-3 week-ends de validation (cron + runProbe + table promo_probe_log à purger).

POINTS EN SUSPENS (fil 27/07, prochain fil ou plus tard) :
- Reco decks dans pickReco : ~~DIFFÉRÉ~~ LIVRÉE (fil 30/07, diffs en revue — cf. section « Fil reco decks & verso deck »). Item reco généralisé source|deck, classement commun sans quota, adopted via ?token= optionnel sur GET /api/collections, adoption par l'endpoint existant, résolution DOM étendue aux tuiles-deck.
- Communauté / réseaux sociaux / forum / alertes perso (« mon chien a disparu ») : à peine ouverts, tension avec l'ADN anti-spam — à cadrer en déclinaison V3 « community » (pré-modération, charte), pas un canal libre.
- Promo Leboncoin : ~~observation en cours ; brancher la vraie alerte après quelques vendredis de données.~~ FAIT (fil 06/09).
- Démontage sonde de contrôle leboncoin (~fin sept après 2-3 week-ends OK) + vérifier transition retour inactive du lundi matin + surveiller le cas pré-annonce (alerte « active » alors que le thread Dealabs précède la promo — nuancer le message si récurrent).
- Boutique de skins : 4 placeholders d'origine (swatch plat) toujours en attente d'illustrations Hugo ; les 9 vrais skins (5 teintes + 4 structurés) sont livrés en CSS pur (cf. « Fil skins de cartes »).
- Points mineurs signalés, non traités : SNCF_API_KEY absente de l'environnement Railway (var à configurer, pas du code) ; pannes-hydro-quebec version bisversion.json illisible (bug source isolé).

## Fil "reco decks & verso deck" (30/07/2026, non déployé — diffs en revue, Hugo pousse/redéploie)

RECO DECKS DANS pickReco (LIVRÉE — lève le point « DIFFÉRÉ » des fils 25/07 et 27/07). Le slot de reco du kiosque recommande désormais des DECKS entiers (officiels + perso publics) mélangés aux sources dans UN SEUL classement par score commun, SANS quota par type (100 % deck, 100 % source ou panaché selon les scores réels). Décisions :
- Item reco GÉNÉRALISÉ `{ type:'source'|'deck', id, score, tiebreak }` (site.js pickReco). Sources = scoring historique INCHANGÉ (dept>intérêt>cats communes>frais>pop). Decks = même esprit que computeSpecialIds : `3*intérêt + 3*cats communes + 2*frais(created_at<30j) + 1*pop(adopt_count normalisé)`, PAS de terme géo (un deck n'a pas de dimension départementale — confirmé sans objet, rien inventé).
- Exclusions deck : `adopted===true` et dismiss (générique par id) ; le privé est déjà filtré en amont par GET /api/collections (officiel+public only).
- adopted : GET /api/collections accepte un `?token=` OPTIONNEL (auth existante, cohérent skins.js/myalerts.js) → colonne `adopted` par deck (EXISTS collection_adoptions) ; sans token, false partout (anonyme inchangé). AUCUNE migration (collection_adoptions préexiste).
- adopt d'un deck recommandé = endpoint EXISTANT POST /api/collections/:id/adopt (le switch de la tuile-deck), rien dupliqué ; dismiss reste générique par id.
- DEUX passages pickReco : 1er au rendu (sources seules, decksData vide) ; 2e après loadDecksIntoGrid() (réconcilie l'UNIQUE reco quand un deck surclasse la source) — non bloquant pour l'affichage initial.
- Résolution DOM généralisée aux tuiles-deck : cardById + placeByOrder couvrent `.card[data-source-id]` ET `.deck-card[data-deck-tile]` ; espace de classement UNIFIÉ (tuiles-deck reçoivent un data-order fractionnaire posé après insertion, sans réécrire l'ordre des cartes). adoptReco/dismissReco fonctionnent tels quels sur une tuile-deck. CSS `.deck-card.card-reco { position:relative }` (ancrage de l'étiquette « Recommandée »).

VERSO DES TUILES-DECK (le « i » retourne la tuile, comme une carte) : la carte de devant se retourne sur un DOS DE DECK — description du deck + catégories + auteur (`par @pseudo`, decks perso ; absent pour officiels) + « Plus d'infos → » vers la page du deck. MÊME structure/CSS/animation que le verso d'une carte classique (backFace) : on REMPLACE le `.card-back` de la source d'aperçu par un verso deck, borne stable `card-back`→`card-share-face` (ordre cardHTML front→back→share, robuste aux div imbriquées). Scope aux TUILES du kiosque (`.deck-card[data-deck-tile]` pointer-events auto sur `.ds-i0 .flip-btn`/`.card-back`) : les aperçus détail/formulaire gardent un « i » inerte. Ruban + éventail masqués pendant le flip (réutilise l'état `ds-sharing` du partage) ; handler flip-back élargi pour retirer ds-sharing au retour ; skip-list de navigation de la tuile étendue (`.ds-i0 .flip-btn`, `.ds-i0 .card-back`).

PÉRIMÈTRE : fichiers touchés = server/routes/collections.js (token optionnel + adopted), public/js/site.js (pickReco, réconciliation, DOM généralisé, flip handlers, opts deck), public/js/deck-stack.js (deckBackHTML + swap du verso), public/js/cards.js (inchangé fonctionnellement — réutilisé), public/css/site.css (pointer-events tuiles, .back-author-name, .deck-card.card-reco). Aucune migration, aucune dépendance, aucune variable d'environnement nouvelle. Vérifs : node --check + harnais Node confirmant le swap du verso (description deck, faces partage/recto intactes, cartes du fond non modifiées).

## Fil "skins de cartes — 9 skins boutique" (non déployé — diffs en revue, migrate à faire ; itéré sur plusieurs tours)

**9 VRAIS SKINS** posés sur le mécanisme boutique existant (`skins`/`user_skins`, `asset_ref` = **token de classe CSS** posé sur `#grid` [dashboard] ou `.deck-card` [deck] ; aucune image, remplaçables sans migration). Seed idempotent `dashboard` dans init.sql (**migrate à faire**). Prix : teintes 30-60, structurés 100-150 (modèle existant). skins-reference.html (racine, non versionné) = spec des 4 structurés, **à supprimer quand validé**.

- **5 TEINTES** (`fullart-or/prisme/braise/ocean/aurore`) : recolorations PURES du skin full-art par défaut — MÊME DOM `.card-front`, MÊME animation « radar calme », seules des VARIABLES CSS changent (`--tc-grad/--tc-motif-stroke/--fa-ring/--tc-edge/--tc-veil` + accent sous-titre). Le VERT d'alerte ne change JAMAIS. Prisme = anneaux multicolores + liseré irisé conic (mask-composite, **fallback @supports** = liseré violet standard sinon rectangle plein qui masquerait la carte). Zéro JS, zéro keyframe, zéro markup.
- **4 STRUCTURÉS** (`ecole-classique/arcane/assemblee/dresseur` = « L'école classique / L'Arcane / L'Assemblée / Le Dresseur ») : approche **STRUCTURE UNIQUE**. `cards.js` émet TOUJOURS un template neutre enrichi (`.card-art` enveloppe motif+voile ; `.card-top`+2 `.card-seal` ; `.card-attacks` ; `.card-foot` ; `.card-action` enveloppe l'action), tout `display:none/contents` par défaut → **aucune branche de rendu, aucun re-render** ; les skins ne font que du CSS scopé par la classe. Chaque skin = frame/plaque/fenêtre d'art/couleurs propres + UNE animation d'état actif (sceau&reflet / boussole / invocation / la charge), jamais partagée, neutralisée par prefers-reduced-motion.

CONTRAINTE N°1 (prouvée, non supposée) : full-art + 5 teintes **PIXEL-IDENTIQUES** avant/après l'enrichissement du template — harnais Playwright, hash SHA-256 égaux clair+sombre, animations figées (le faux positif initial était la phase du point vert animé). À RELANCER si cards.js retouché.

PIÈGES & DÉCISIONS D'ARCHI (à retenir) :
- **Spécificité** : la base `.grid .card-front .X` (0,3,0) bat un naïf `.skin-X .X` (0,2,0) → tous les sélecteurs structurés qui touchent `.card-edge/.card-veil/.card-subtitle/.card-front` sont scopés sous `.card-front` (0,3,0, victoire par ordre source). Bug typique symptôme : panneau crème du Dresseur par-dessus le contenu (z-index base gagnait).
- **RATIO 2/3 PARTOUT (règle finale)** : une tentative de croissance `height:auto/min-height:360` (pour loger les cartes paramétrées) a été **ANNULÉE** car elle déformait les tuiles-deck (faces au 2/3). Les 4 structurés gardent `aspect-ratio:2/3` dans TOUS les contextes (grille, decks, boutique). Le **débordement** (paramétrées multi-chips, descriptions longues) est géré par **DÉGRADATION CSS PURE** : régions flexibles (fenêtre d'art, boîte de texte, médaillon, attaques) en `grid-template-rows: … minmax(0,…) …` + `overflow:hidden` → la description/ambiance se clippe PAR LE BAS (l'ambiance `::after` part avant la description) ; la **ZONE D'ACTION** (chips/« +ajouter »/select/switch/état/icônes) est en ligne `auto` → **JAMAIS tronquée**. Ordre de sacrifice : décoratif (ambiance, pied Dresseur, médaillon Arcane rendu responsive `height:100%;aspect-ratio:1`) → description → jamais le fonctionnel. **Aucune classe JS nécessaire.**
- **Dresseur = contenu RÉEL** : `attacksHTML(s)` (cards.js) émet la VRAIE description de la source en « effet d'attaque » (1 rangée, nom neutre « Signal », dégâts = likes réels, masqués si 0). AUCUNE chaîne sismique en dur (bug initial : constante figée « Détection/Onde sismique » sur toutes les cartes). `.card-desc` réel reste dans le DOM en **sr-only** (a11y : l'effet d'attaque est `aria-hidden` = écho visuel). PV/espèce = sous-titre réel repositionné ; « PV 120 » décoratif générique (non-sismique) assumé.
- **DECKS SKINNÉS** : le skin dashboard (`#grid`) s'applique AUSSI aux aperçus des tuiles-deck (`faceHTML` → `LBACards.cardHTML` → vrais `.card-front`). Décision INVERSÉE en cours de route (d'abord exclus via un scope `> .card`, puis **inclus** — « les decks sont des assemblages de cartes »). Le 2/3 garantit une géométrie de tuile **identique au défaut** (hauteurs mesurées toutes égales, 345px à 230px de large). Ruban de deck = **centrage d'origine** conservé (un ancrage-bas testé puis retiré, inutile une fois le 2/3 rétabli).
- **Boutique** : aperçu = **VRAI RENDU** (pas d'image figée). Teintes en `.has-card` (mini carte-front 2/3, radar réel animé) ; structurés en `.has-card-full` (vraie carte à géométrie naturelle scalée par `transform:scale` depuis `transform-origin:top-left` dans une boîte fixe DE LA TAILLE SCALÉE — sinon la boîte pleine taille débordait/clippait au centrage).
- **Icônes** (like/share/+deck/i) restent en **absolu haut-droite** (décision actée) ; l'état est en overlay haut-gauche (sauf Dresseur : retiré du haut, l'état vit dans « retraite »).

FICHIERS : public/js/cards.js (template neutre + `attacksHTML(s)` + wrapper `.card-action`), public/css/site.css (gros bloc : défauts neutres inertes, 5 teintes, 4 structurés jour/nuit, dégradation 2/3, aperçus boutique), public/js/boutique.js (aperçus vrai-rendu), server/db/init.sql (seed 9 skins dashboard idempotent). **À FAIRE : `node server/db/migrate.js` (seed), puis déploiement app.** Micro-lot nettoyage UI noté en mémoire (pastille d'état parfois tronquée [pré-existant] + retrait handler reveal mobile inerte).

## Fil "V3 — tâche à échéance glissante" (30/07/2026 ; carte activée en base de TEST, prod à confirmer)

PREMIÈRE BRIQUE DE LA FAMILLE V3 « MAINTENUES » (roadmap 5). Carte `tache-echeance-glissante`, **NOUVEAU type de source `user-task`** : elle ne surveille RIEN (aucune API, aucun état de veille, pas de ligne source_states). L'utilisateur déclare une échéance récurrente (vidange, contrôle technique, détartrage…) avec sa dernière réalisation + sa périodicité ; relance à l'approche ; un clic « c'est fait » repositionne l'échéance **à partir d'aujourd'hui** (elle GLISSE au rythme réel, pas au calendrier théorique). Seedée `enabled = false` dans init.sql — activation = `UPDATE sources SET enabled = true WHERE id = 'tache-echeance-glissante';` (geste produit manuel, jamais dans init.sql).

DB (migrations idempotentes dans init.sql) : table **user_tasks** — BIGSERIAL, `subscriber_id` INTEGER REFERENCES subscribers(id) ON DELETE CASCADE (⚠️ il n'y a PAS de table `users` dans ce schéma), label VARCHAR(80), tracking_mode 'time'|'counter', anchor_date/periodicity_value/periodicity_unit/next_due, counter_* (mode counter, non implémenté), announce_days DEFAULT 7, last_confirmed_at, **confirm_token VARCHAR(64)**, last_notified_at, active. 6 CHECK idempotents (pattern pg_constraint), 4 index dont **unique partiel (subscriber_id, label) WHERE active = true** (anti double-clic, PAS un quota — le soft delete libère donc le libellé). PAS D'UUID : aucun UUID nulle part dans le schéma et `gen_random_uuid()` dépendrait d'une version PG non vérifiée → BIGSERIAL + token hex 64 `crypto.randomBytes(32)`, comme subscribers.token / sessions.token.
- ⚠️ `params_schema` reste **NULL** (surtout pas `'[]'::jsonb`) : le poller range dans le chemin paramétré toute source dont params_schema est non-NULL (`paramIds = rows.filter(r => r.params_schema != null)`).
- Exclusion EXPLICITE du cycle : `WHERE enabled = true AND type NOT IN ('linked','user-task')` dans runCycle. L'absence de fichier dans server/sources/ suffisait de fait, mais un ajout accidentel aurait cassé en silence.

JOB DE RELANCE (server/user-tasks-notify.js, `runUserTaskNotify(pool)`) : **2e cron dédié du projet** après la sonde Leboncoin — `0 9 * * *` Europe/Paris dans startPoller. Justification assumée : une échéance se compte en jours, le cycle de 30 min enverrait à une heure arbitraire (y compris la nuit). Garde ANTI-SPAM = `last_notified_at < (next_due - announce_days)` → UNE relance par échéance, et une confirmation qui repousse next_due rouvre le droit à une relance sans remise à NULL. Filtres destinataires standards (`confirmed`, `email_enabled`). Heures de veille : on **SAUTE** (pas de différé — deferred_notifications a un source_id et un digest au format liste d'alertes, inadapté à un mail à bouton d'action) ; le balayage du lendemain reprend. Redémarrage Railway à 9h05 = relance décalée d'un jour, assumé (pas de cron de rattrapage pour un cas limite). CHOIX PRODUIT ASSUMÉ : **aucune relance de retard** — une tâche notifiée puis jamais confirmée reste silencieuse indéfiniment.

CONFIRMATION — DEUX PORTES, UNE SEULE LOGIQUE (`applyTimeConfirmation`, server/routes/user-tasks.js) : (a) lien email `GET /tache/:id/confirmer/:token` (pagesRouter, pattern subscribe.js — un client mail ne fait JAMAIS de POST au clic) ; (b) bouton in-app `POST /api/user-tasks/:id/confirm` (token de session en corps JSON — le projet n'a AUCUN middleware d'auth, chaque route appelle `authenticate()` à la main). Le token est régénéré à chaque confirmation → usage unique de fait, l'ancien lien devient invalide. PAS de TTL sur confirm_token (cohérent avec subscribers.token ; le pire cas est de confirmer une tâche perso, aucune donnée sensible). Décalage calendaire confié à **make_interval** (PG natif : gère 31→30 et les bissextiles sans code maison) ; dates en UTC serveur, négligeable à cette granularité.

API : `POST /api/user-tasks` (création, tracking_mode forcé à 'time' côté serveur — le client ne le transmet jamais), `POST /api/user-tasks/:id/confirm`, `DELETE /api/user-tasks/:id` (**SOFT DELETE** `active = false`, jamais de suppression de ligne). Propriété vérifiée dans le WHERE → 404 et jamais 403 (pas d'énumération). Rate-limit mémoire 20 créations/h/compte (pattern rateOk de decks.js). 23505 → 409 « Vous suivez déjà une tâche identique » (jamais l'erreur PG brute).

VALIDATION — `ugc.validateTaskLabel` + `ALLOWED_TASK_LABEL` : `validateText` accepte désormais une option `allowed` (jeu de caractères substituable, défaut inchangé pour decks/pseudos). Jeu ÉLARGI pour les libellés de tâche (usage domestique : « Vidange (voiture) », « Rév. 20 000 km », « Chaudière + adoucisseur ») = ajout de `. , ( ) ° + & :` — **volontairement ni `/` ni `@`**, pour rester STRICTEMENT aligné sur le CHECK SQL `user_tasks_label_chk (label !~ '[/@]')` : laisser passer côté Node ce que la base refuse ferait remonter l'erreur PG brute que la validation existe pour éviter.

FRONT — 5e FACE DE CARTE (structure généralisable, nommage volontairement générique) : `.card-face.card-task-face` pilotée par `.face-task`, même mécanique exacte que card-share-face/card-deck-face (`.card.face-task .card-back {display:none}` + `.card:not(.face-task) .card-task-face {display:none}`) ; les 4 énumérations CSS partagées (dégradé/liseré/titre/vue liste) ont été complétées ; `face-task` ajouté au handler `.flip-back` (sinon la face resterait affichée au retour). **RECTO = structure identique aux cartes v2 paramétrées** : bouton d'action (« Configurer », pastille style .param-add) PUIS `.switch-row.param-mute-row`. Le ⓘ garde son verso classique (description/tags/infos, aucune trace de tâche). La 5e face porte : titre, liste des tâches (échéance, « C'est fait ✓ », croix de suppression sans confirmation façon chip de deck), bouton « Créer ma tâche »/« + une autre tâche ». État recto = 3e vocabulaire neutre `.state.task` (« Tâche personnelle » / « Tâche suivie ») et **JAMAIS `.state.active`** : cette classe anime le motif full-art (`:has(.state.active)`) et alimente le KPI « mes alertes actives » via cardIsActive() — une échéance suivie n'est pas une alerte en cours.

FORMULAIRE (public/js/user-task-form.js) : modale réutilisant le gabarit share.js (.share-modal-backdrop/-card, croix + clic fond + Échap + focus). ANTI-HALLUCINATION STRICTE : **aucune durée n'est jamais devinée** — le nombre de la périodicité reste vide et obligatoire, seuls sont pré-posés une UNITÉ par défaut ('mois'), un `placeholder="ex : 6"` (jamais une `value`) et la date du jour comme dernière réalisation (défaut ergonomique neutre, `max` = aujourd'hui). Suggestions de libellés (« Contrôle technique », « Vidange »…) = TEXTE SEUL, jamais des presets de durée. Préavis borné par la période elle-même (30 j/mois, 365 j/an — borne de SAISIE, pas un calcul d'échéance). ⚠️ le `<form>` porte `novalidate` → `required` ne bloque PAS la soumission : garde-fou client explicite ajouté.

PAUSE : `subscriptions.muted` RÉUTILISÉ tel quel (toggle-mute est générique `(subscriber_id, source_id)`, aucun lien avec source_param_states malgré son commentaire) → zéro nouvelle route, zéro nouveau handler, zéro nouvelle colonne. Contrepartie : il FAUT une ligne subscriptions (params NULL, INERTE pour le poller) — créée à la création d'une tâche, **backfill idempotent dans init.sql** pour les tâches antérieures, et SUPPRIMÉE quand la dernière tâche part (sinon carte comptée dans « Ma collection » et subscriber_count sans rien derrière). Effet de bord assumé : le réglage de pause est perdu si l'utilisateur recrée une tâche.

DIVERS : `htmlPage` (subscribe.js) passée en CHARTE VIOLETTE (elle était restée sur l'ancienne palette ambre `#d98e04`/`#faf8f5` — les pages /confirm et /unsubscribe étaient hors charte depuis la bascule, personne ne l'avait vu) + nouveau paramètre optionnel `headExtra`. `sendTaskDueReminder` (mailer.js, via emailShell) : `fallbackUrl` retiré (le lien de repli portait un token de 64 caractères). Script `scripts/test-user-task-notify.js` (déclencheur manuel du job, `pool.end()` en finally — sans lui le shell Railway ne rend jamais la main). ⚠️ ENVOIE DE VRAIS EMAILS.

DETTES OUVERTES (toutes non bloquantes) : (1) ~~VUE LISTE — PRIORITAIRE~~ **RÉSOLUE le 05/08/2026** (voir ci-dessous « vue liste — tâche à échéance ») ; (2) mode `counter` sans UI de relevé, colonne counter_target morte ; (3) `htmlPage` n'échappe pas ses interpolations (escapeHtml local dans user-tasks.js — à mutualiser au 2e appelant qui injectera du variable) ; (4) pas de plafond d'affichage des tâches sur la face (défilement natif comme seul filet) ; (5) push non branché sur « c'est fait » ; (6) pas de relance de retard (choix produit) ; (7) pas de TTL sur confirm_token (choix produit) ; (8) titre « Mes échéances » en dur — à généraliser à la 2e carte de ce type, pas avant ; (9) pause invisible depuis la face tâches (risque « j'ai mis en pause et j'oublie », à réévaluer si rapporté) ; (10) mes-decks.html charge user-task-form.js par uniformité alors que ses cartes sont des aperçus `pointer-events:none`.

## Fil "vue liste — tâche à échéance" (05/08/2026, non déployé — diffs en revue ; solde la dette n°1 du fil V3)

PRINCIPE : **aucune logique métier ajoutée**. On étend à `user-task` le patron liste qui existait déjà pour les cartes paramétrées — le « parking de carte » (B1) : le corps de ligne déplie le **nœud DOM réel** de la carte de #grid dans `.lrow-exp`, donc tous les handlers de site.js (délégués sur `document`) et la modale user-task-form.js continuent d'opérer nativement. Zéro formulaire de liste, zéro fetch dupliqué.

- **list-view.js** : ajout de `isUserTask()` et d'un prédicat commun `hasBlocks() = isParam() || isUserTask()`, substitué aux trois `isParam()` qui testaient la SÉMANTIQUE (switch de la ligne fermée, sync(), ouverture du volet) et non le schéma. Un `user-task` n'a pas de `params_schema`, mais son contrôle a exactement le sens d'une carte paramétrée : **actif = ≥1 instance ET non en pause**, OFF = pause (jamais un retrait destructif). Seule différence de porteur : `s.tasks` au lieu de `s.instances`.
- Switch ON depuis « aucune tâche » : impossible d'abonner sans échéance → on déplie et on ouvre **la modale de la vue cartes** (`LBATaskForm.open`) sur la carte parquée.
- La 5e face est ouverte d'emblée à l'ouverture du volet (classe `.face-task`, la même qu'en vue cartes) : le recto ne porte que « + configurer », qui ne servirait qu'à ouvrir cette face. Garde-fou anonyme : `taskFace()` ne rend rien hors connexion, on ne pose donc `.face-task` que si `.card-usertask-face` existe.
- **MutationObserver** sur la carte parquée (posé par `parkCardInto`, coupé par `unparkCard`) : toute mutation de la carte resynchronise le switch de la ligne. Remplace les `setTimeout(sync, 400)` à l'aveugle pour les mutations asynchrones et hors de #list (la modale vit sur `<body>`). Pas de boucle possible : `sync()` n'écrit que dans la LIGNE. `unparkCard()` retire aussi `flipped/face-*` pour que la carte revienne à la grille sur son recto.
- **cards.js** : une seule ligne — marqueur ADDITIF `.card-usertask-face` sur la 5e face de `taskFace()`. Structure et classes inchangées (skins compris). Nécessaire parce que les cartes `community` réutilisent `.card-task-face` : la vue liste dévoile la face des tâches, pas celle des signalements.
- **site.css** : exception à `.lrow-exp .card.in-list-exp .card-task-face { display: none }`, ciblée `.card-usertask-face`. Comme `.card-inner` est figé (`transform: none`) dans le volet, le flip ne tourne pas — on rejoue la bascule **en display**, pilotée par la même classe `.face-task`, donc par les mêmes handlers. `.flip-back` et `.tf-title` masqués dans le volet (doublons du nom de ligne / retour vers un recto sans contenu).

RESTE : ~~la carte `community` n'a toujours aucun chemin en vue liste~~ **RÉSOLU le 14/08/2026**, cf. section suivante. La prévision de ce fil (« il suffira d'ajouter `isCommunity` à `hasBlocks()` ») s'est révélée **à moitié fausse** : `isCommunity` a bien été ajouté à `list-view.js`, mais **délibérément HORS de `hasBlocks()`** — voir pourquoi ci-dessous.

## Fil "vue liste — carte communautaire" (14/08/2026, DÉPLOYÉ ; solde le RESTE du fil précédent)

Troisième et dernière application du patron « parking de carte » (B1), après les paramétrées puis `user-task`. **Aucune route, aucune logique métier, aucun formulaire touché** — le corps de ligne déplie le nœud DOM réel de la carte, tous les handlers délégués de site.js opèrent nativement.

⚠️ **CORRECTION DE LA PRÉVISION DU FIL PRÉCÉDENT — `isCommunity` est VOLONTAIREMENT hors de `hasBlocks()`.** `hasBlocks()` pilote la SÉMANTIQUE DU SWITCH : « actif = ≥1 instance ET non en pause ». Or le switch d'une carte communautaire est un **abonnement broadcast classique** (`s.subscribed`, `POST /api/my-alerts/toggle`), sans notion d'instance ni de pause — l'y ranger aurait fait piloter le switch par la branche instance/pause et affiché un état faux. `isCommunity()` n'est donc consulté **qu'au point d'ouverture du volet** (`openParams`), où corps de ligne et switch sont déjà traités séparément. Leçon : la liste distingue **ce qui se déplie** de **ce que le switch signifie** — deux questions à trancher séparément pour tout nouveau type.

- **cards.js** : marqueur ADDITIF `.card-community-face` sur `communityReportsFace()`, miroir exact de `.card-usertask-face`. Nécessaire parce que les deux familles partagent `.card-task-face` : sans lui, le volet dévoilerait la face des tâches au lieu de celle des signalements.
- **site.css** : exception symétrique à celle de `user-task` (bascule rejouée EN DISPLAY, `.card-inner` étant figé dans le volet).
- **`CACHE_VERSION` bumpé à `v3`** (réflexe acquis au fil Chat perdu, cf. piège `sw.js`).

TESTÉ EN PROD : ouverture du volet, les 3 états du recto rendus dans le volet, switch correct dans les deux sens.

## Fil "cartes communautaires — Chat perdu" (04-14/08/2026 ; DÉPLOYÉ, MIGRÉ, testé manuellement en prod bout en bout — CHANTIER CLOS)

PREMIER TYPE d'une future FAMILLE de cartes communautaires. Contrairement à `user-task` (contenu PRIVÉ, une ligne par compte), une carte communautaire est un contenu **PARTAGÉ entre tous les abonnés d'une même zone**. Premier type : `chat-perdu`, `sources.type = 'community'`, badge `community`, catégorie `communaute`, display_order 500 (un 2e type, `chien-perdu`, a été ajouté le 15/08 — cf. fil « généralisation des cartes communautaires » plus bas ; tout ce qui suit dans CE fil décrit le socle, resté valable). La ligne `sources` est VIRTUELLE : gabarit de picker + entrée de catalogue, jamais de ligne `source_states`, jamais de fichier dans server/sources/ (le poller ne la traite jamais).

Déroulé du fil : cadrage → schéma/routes (lot 1) → client lot 2a → refonte V3 recto/verso → 4 vagues de corrections (dont un incident prod, cf. ci-dessous) → Vague 2 = distinction auteur/témoin + notification email → clos.

**SCHÉMA** (init.sql, idempotent) : `community_reports` (BIGSERIAL, type, commune_nom/commune_insee, lat/lon, `radius_km` choisi par l'auteur à la création [10-50 km, défaut 15], description, link, author_subscriber_id, created_at, expires_at, `extensions_count`, status CHECK `active|resolved|expired`) + index partiel `(type, status) WHERE status='active'` ; `community_report_spots` (PK composite `report_id, subscriber_id` = dédup naturelle du « Je l'ai vu », + `seen_where TEXT`/`seen_when TEXT` nullables ajoutées en Vague 2 — obligatoires côté route, pas en contrainte SQL, car des spots antérieurs à cette fonctionnalité existent sans ces champs) ; `community_report_subscriptions` (même PK composite, abonnés directs, auteur inclus dès la création) ; `subscribers.hide_community_reports BOOLEAN NOT NULL DEFAULT false` (préférence Mon compte). `params_schema` de la ligne catalogue = un seul paramètre `ville` de type `commune-coords` (v2 = un seul paramètre par schéma) : description/lien/rayon sont saisis À PART par la route, hors params_schema. `forum_slug = 'chatperdu'` posé À LA MAIN dans init.sql (guard `IS NULL`, même règle de calcul que `forum-slug.js`) — cette ligne ayant été insérée directement en SQL, ni `server/forum-slug.js` (insertion dynamique) ni `scripts/backfill-forum-slug.js` (rattrapage) ne l'avaient jamais posé ; badge `@chatperdu` + lien forum absents jusqu'à ce correctif.

**RÈGLES MÉTIER** : dédup par commune (code INSEE si résolu, sinon nom normalisé) — **spécifique à ce type**, décidée côté serveur (le client ne pré-teste rien, un seul `POST` couvre création ET adhésion). Rate-limit **1 signalement actif par auteur à la fois** (vérification applicative `SELECT COUNT(*)` avant `INSERT`, pas de contrainte SQL — aucun précédent de ce genre dans le repo). Expiration 7 j, prolongeable par l'auteur (`POST /:id/extend`, max 3 fois). Fermeture : automatique au 3e « Je l'ai vu » distinct, ou manuelle par l'auteur via « Je l'ai retrouvé » (`POST /:id/resolve`, réutilisée telle quelle).

**ROUTES** (`server/routes/community-reports.js`, monté séparément sous `/api` — **PAS de fusion dans `/api/sources`**, volontairement, pour ne pas alourdir cette route critique) : `GET /api/community-reports` (instances visibles = abonnements directs + tout profil dans le `radius_km` de l'instance [coordonnées du profil résolues à la volée, jamais stockées], sauf `hide_community_reports` ; renvoie aussi `subscribed`/`spotted`/`is_author` par ligne, calculés côté serveur — le client n'a **aucun accès à son propre id de subscriber**, donc `is_author` est indispensable, pas une simple commodité) ; `POST /api/community-reports` (crée ou rejoint) ; `POST /:id/spot` (« Je l'ai vu », Vague 2 : `where`/`when` **obligatoires**, validés via `ugc.validateText`/`ALLOWED_FORUM_BODY`, déclenche l'email à l'auteur) ; `POST /:id/extend` ; `POST /:id/resolve`. Géo : `sources/lib/commune-insee.js` (résolution) + `sources/lib/geo-distance.js` (`distanceKm`, Haversine — aucune fonction de distance n'existait avant ce chantier).

**GARDE-FOUS UGC** : description validée via `ugc.validateText(..., { allowed: ugc.ALLOWED_FORUM_BODY })` (ponctuation complète, pas le charset `ALLOWED_TEXT` par défaut trop restrictif pour plusieurs phrases) ; lien optionnel validé par **liste blanche de domaines** (`ugc.ALLOWED_LINK_DOMAINS` = i-cad.fr, filalapat.fr, spa.asso.fr, 30millionsdamis.fr — premier mécanisme de liste blanche du projet, message d'erreur énumère les domaines autorisés).

**JOB** (`server/community-reports-expire.js`, `runCommunityReportsExpire(pool)`) : 3e cron du projet, `0 4 * * *`. Ne notifie personne — passe en `expired` les instances `active` dont `expires_at` est dépassé.

**EMAIL** (Vague 2, `server/mailer.js`, `sendCommunityReportSpotNotification`) : calquée à l'identique sur `sendForumReplyNotification` (même `FROM`/`emailShell`/compteurs). Envoyée à l'auteur dès le **1er** « Je l'ai vu » (pas besoin d'attendre le seuil de fermeture à 3), avec le `where`/`when` du témoin. **Best-effort strict** : déclenchée APRÈS `res.json()` dans la route, jamais `await`ée par la réponse HTTP, `.catch()`-only — un échec d'envoi ne peut jamais faire échouer `/spot`. Jamais envoyée si l'auteur se spot lui-même, ni sur un spot déjà existant (idempotence).

**FRONT — carte V3 recto/verso, calquée sur `user-task`** : le bouton « Signaler » réutilise **la même classe `.task-config`** (+ `.community-config` en second marqueur pour son fetch dédié) → le handler générique de flip de site.js ouvre la 5e face sans câblage supplémentaire. La 5e face réutilise `.card-task-face`/`.tf-title`/`.task-zone` telles quelles ; liste des signalements (`communityReportItem`, une seule fonction réutilisée recto ET verso) + formulaire de création/adhésion inline (masqué par défaut, révélé par `.community-report-toggle`). **Recto DYNAMIQUE (3 états)**, calculé à chaque chargement de la home ET à chaque ouverture de la 5e face (jamais figé au 1er fetch de la session) :
  1. Aucune instance visible → générique « Rien à signaler » (comportement broadcast classique inchangé) ;
  2. Instance(s) visible(s), abonné témoin → item affiché (commune/description/expiration), bouton **« Je l'ai vu »** qui révèle un mini-formulaire Où/Quand obligatoire avant tout envoi (jamais un clic immédiat) ; rotation automatique 10 s entre plusieurs instances, même vocabulaire de transition que le H1 de `/favoris` (`bd-slide-out`/`bd-slide-in`) ;
  3. Instance visible, abonné auteur → même item, bouton **« Je l'ai retrouvé »** à la place (clôture directe, aucun champ).
  Le switch d'abonnement au recto reste le mécanisme **classique** (`switchRow`/`toggleConnected`, `POST /api/my-alerts/toggle`, source_id='chat-perdu' sans params) — totalement indépendant des signalements eux-mêmes, c'est juste « être alerté quand quelque chose se passe sur cette carte » comme pour toute autre carte broadcast. État recto neutre `.state.task`, jamais `.state.active` (même raison que `user-task`).

**BUGS CORRIGÉS EN COURS DE FIL** (base fiabilisée avant la Vague 2) : piège `[hidden]` rencontré et corrigé **trois fois** sur ce chantier (`.community-form`, `.community-report-toggle`, `.community-spot-toggle`) — une règle CSS auteur `display:` sur un élément l'emporte sur l'attribut `[hidden]` (origine UA) sauf override explicite `.classe[hidden]{display:none}` ; réflexe désormais systématique dès l'écriture pour tout nouveau bouton masquable. `note()` ne doit jamais être utilisé pour un message posé pendant l'interaction sur le VERSO d'une carte (il écrit dans `.card-front .card-content`, invisible tant que la carte est retournée) → message dédié `.community-form-msg`/`.community-spot-msg` (classe `sub-msg` réutilisée pour les couleurs) directement dans la face concernée. Un « bug d'affichage qui ne se corrige pas malgré un fix appliqué » s'est révélé être `public/sw.js` : `CACHE_VERSION` jamais incrémenté malgré de nombreuses éditions de `cards.js`/`site.js`/`site.css` → le service worker (stale-while-revalidate sur les assets statiques) servait du JS/CSS obsolète à certaines sessions. Bumpée à `v2` — **réflexe à prendre systématiquement pour toute future session de corrections client** : bump `CACHE_VERSION` en fin de fil, pas seulement au déploiement final.

**TESTÉ MANUELLEMENT EN PROD, BOUT EN BOUT** (confirmé par Hugo) : création, dédup par commune, rate-limit un-actif-par-auteur, « Je l'ai vu » avec Où/Quand + email, seuil de fermeture automatique à 3, prolongation plafonnée à 3, clôture manuelle et automatique, les 3 états du recto, badge/lien forum. Données de test nettoyées après validation.

**RESTE À FAIRE — LES 3 SONT DÉSORMAIS SOLDÉS** : (1) ~~**vue liste**~~ **FAIT le 14/08/2026**, cf. fil « vue liste — carte communautaire » ci-dessus (avec une correction de prévision : `isCommunity` est hors de `hasBlocks()`) ; (2) ~~**généralisation à d'autres types de cartes communautaires**~~ **FAIT le 15/08/2026** — cf. fil dédié plus bas ; la dédup par commune est bien devenue un réglage par type (`dedupByCommune`) et non un comportement câblé ; (3) ~~**audit `forum_slug`**~~ **FAIT le 14/08/2026** — cf. fil « audit forum_slug préventif » ; l'audit a conclu que `chat-perdu` était le SEUL cas et qu'il était déjà réparé, mais il a corrigé au passage une premisse fausse sur la cause.

### INCIDENT PROD (14/08/2026) — sources.description écrasée par un UPDATE sans WHERE

En voulant corriger la seule ligne `chat-perdu`, un `UPDATE sources SET description = '...'` a été exécuté **sans clause WHERE** dans l'éditeur SQL Railway → la colonne `description` de **TOUTES** les sources a été écrasée avec le texte destiné à `chat-perdu`. `server/db/init.sql` restait intact (chaque `INSERT` porte la vraie description d'origine) mais un simple re-run de `migrate.js` ne répare rien : les `INSERT ... WHERE NOT EXISTS` ne se rejouent jamais sur un id déjà présent.

RÉPARATION : script one-off `scripts/_repair-2026-08-14-descriptions.js` (supprimé après usage, non versionné à terme) — rejoue textuellement les 291 blocs `INSERT INTO sources (...) SELECT ... WHERE NOT EXISTS (...)` d'init.sql dans une table `TEMP` (seule transformation : nom de table cible + guard interne repointés sur la table temp, aucune autre modification du texte des requêtes), pour reconstituer les valeurs d'origine sans dépendre de l'état de la vraie table. Mode `--check` obligatoire avant tout `--apply` (diff id par id, rollback systématique, aucune écriture). Garde-fou de cohérence (abandon si la table temp couvre <90 % des sources réelles). Exclusion explicite des sources ayant une révision éditoriale légitime plus récente qu'init.sql (7 cas détectés au `--check` : arrosage-canal-gap, grands-anniversaires, jours-feries, panneaupocket, rappel-conso, vigicrues-05, vigieau) — signal de corruption réelle retenu : la description actuelle contient le texte de l'incident (regex `chat dispara[iî]t`), pas un simple écart avec init.sql. Vérification post-apply (0 description suspecte restante hors `chat-perdu`) avant `COMMIT`. **283 descriptions restaurées, 0 perte de données** (init.sql est resté la source de vérité de bout en bout).

LEÇON RETENUE : ne plus jamais exécuter d'`UPDATE` en direct dans l'éditeur SQL Railway sans clause WHERE vérifiée au préalable, même pour un correctif jugé trivial en urgence — repasser par un script avec mode `--check` systématique, y compris pour les corrections « rapides ».

## Fil "généralisation des cartes communautaires" (15/08/2026 ; CODÉ ET TESTÉ, **PAS ENCORE DÉPLOYÉ NI MIGRÉ**)

Clôture la dette (2) du fil Chat perdu. Cas de test choisi par Hugo : un 2e type **séparé**, `chien-perdu`, structurellement quasi identique à `chat-perdu` — volontairement le cas le plus exigeant, le critère d'échec étant « si l'ajout force un copier-coller de `community-reports.js`, la généralisation a échoué ». Elle n'a pas été forcée : **aucun fichier dupliqué, aucune route ajoutée**.

CONSTAT D'EXPLORATION : la colonne `community_reports.type` était **réellement exploitée** dès l'origine (filtre `WHERE r.type = $n` présent dans les 4 requêtes qui en ont besoin, `INSERT` explicite, index menant par `type`) — le multi-types était anticipé, pas oublié. Le seul verrou était `const TYPE = 'chat-perdu'` en tête de routeur, plus le fait que le client n'envoyait **jamais** de type. `/spot`, `/extend`, `/resolve` étaient déjà type-agnostiques (adressage par id), tout comme le cron d'expiration et `list-view.js`.

**`server/community-types.js` (NOUVEAU) = SOURCE DE VÉRITÉ de tout ce qui varie par type.** Données JS, pas de table (cohérent avec le catalogue `sources`, édité à la main). Par type : `label` (le mot de l'animal), `emoji`, `emailHeading`, `dedupByCommune`, `expireDays`, `maxExtensions`, `spotsToClose`, `defaultRadiusKm`/`min`/`max`/`radiusChoices`, et un bloc `catalog` (gabarit de la ligne init.sql). **AJOUTER UN TYPE = 1 entrée ici + 1 ligne catalogue dans init.sql. Rien d'autre.**
- `getType(raw)` = **liste blanche stricte** (rejette tout non-string et toute clé inconnue) → 400. Une valeur cliente n'est JAMAIS insérée telle quelle : c'est toujours `cfg.id`, relu du module, qui part en base.
- `getType(null)` renvoie `chat-perdu` **délibérément** : un onglet ouvert pendant le déploiement n'envoie pas de `type` et ne doit pas voir ses signalements refusés. Même repli côté `cards.js` et `mailer.js`.
- `publicConfig(id)` = sous-ensemble transmis au navigateur (**libellé + rayons uniquement**) ; les règles métier (dédup, expiration, seuil de clôture) ne quittent jamais le serveur. `attachConfig(row)` l'agrafe à une ligne `sources`.

**D'OÙ VIENT LE TYPE, SELON LA ROUTE** (distinction importante, pas un détail d'implémentation) : à la **création/lecture** (`POST /`, `GET /?type=`) il vient du client et passe par la liste blanche ; sur **`/spot` et `/extend`** il est **relu sur la ligne elle-même** (`cfgOfRow`) — sinon un témoin pourrait emprunter les réglages d'un autre type (seuil de clôture, durée de prolongation) en envoyant le `type` de son choix.

**PORTÉE DES MODIFS** : `community-reports.js` (plus aucune valeur propre à un type ; `if (cfg.dedupByCommune)` autour du bloc dédup, la création directe étant le chemin par défaut) · `api.js` (`/api/sources` **et `/api/favorites`**) · `collections.js` (une carte communautaire dans un pack doit parler le vocabulaire de SON type — hors périmètre initial, ajouté au constat) · `cards.js` (2 `aria-label` + rayons lus dans la config ; nouveaux attributs `data-card-type="community"`, `data-community-type`, `data-community-label`) · `site.js` (**sélection par FAMILLE, plus par id**) · `mailer.js` (3 emplacements) · `init.sql` (ligne `chien-perdu`, display_order 501).

⚠️ **PIÈGE ÉVITÉ, À RETENIR POUR TOUT FUTUR TYPE** : `site.js` sélectionnait le recto dynamique par `.card[data-source-id="chat-perdu"]` — un id en dur. Tout nouveau type aurait été **silencieusement** privé de recto dynamique (aucune erreur, juste une carte statique). Désormais `.card[data-card-type="community"]`. Le seul autre texte « chat » du JS était dans des **`aria-label`** — invisible à l'œil, donc invisible à une relecture visuelle : à chercher explicitement.

DÉCISIONS ACTÉES (Hugo) : rate-limit « 1 actif par auteur » **reste PAR TYPE** — assouplissement assumé, un compte peut avoir 1 chat-perdu ET 1 chien-perdu actifs simultanément ; `radius_km` non paramétrable par type tant qu'aucun type ne l'exige. `ugc.ALLOWED_LINK_DOMAINS` inchangée (i-cad/filalapat/SPA/30MA couvrent déjà les chiens).

CORRECTIF DE FOND AU PASSAGE : `expires_at` interpolait `EXPIRE_DAYS` **dans la chaîne SQL**. Sans risque avec une constante, moins avec une valeur de configuration → passé en `make_interval(days => $n)`, paramétré.

**VÉRIFICATIONS — 69 assertions, toutes vertes** : (a) **routes 35/35**, vrai code de route + vraie base, le tout dans une **transaction rollbackée** (technique du fil « intégrité likes » : `db.pool.query` repointé sur le client de la transaction AVANT le `require` du routeur, qui garde la référence de l'objet `pool`) — non-régression chat-perdu complète (création, dédup, rate-limit, spot, idempotence, extend, resolve, expiration à 7 j), chien-perdu complet, **étanchéité entre types dans les deux sens**, liste blanche (type inconnu / objet / `"chat-perdu'; DROP TABLE sources;--"` → 400, table `sources` intacte), compat sans `type` ; (b) **emails 13/13** — payload réellement composé par `mailer.js`, intercepté au niveau du SDK Resend (prototype de `Emails` patché avant chargement), vérifié notamment qu'aucun email ne contient le mot de l'AUTRE animal ; (c) **front 21/21** — `cards.js` chargé dans Chromium, DOM inspecté, dont la démonstration directe de la régression évitée (sélecteur par famille = 2 cartes, ancien sélecteur par id = 1).

⚠️ **ÉCRITURE PROD ACCIDENTELLE PENDANT LES TESTS, CORRIGÉE** : `incEmailCounters()` écrit dans `counters` **hors** de la transaction de test → les 3 emails simulés ont gonflé les compteurs de prod. Détecté, corrigé (`emails_total` 93→90, `emails_202608` 32→29), invariant `total = somme des mois` revérifié avant COMMIT. **LEÇON : une transaction rollbackée n'isole que ce qui passe par le `pool` patché — tout effet de bord qui rouvre sa propre connexion (compteurs, jobs) y échappe.** À vérifier explicitement pour tout futur test de ce genre.

MIGRATION : **rien à migrer sur les données existantes** — aucun `ALTER`, aucun backfill, le refactor ne change ni le schéma ni les valeurs stockées, seulement le code qui les lit/écrit. Les lignes `chat-perdu` existantes portent déjà `type = 'chat-perdu'`. La seule écriture est l'`INSERT` de la ligne catalogue, gardé par `WHERE NOT EXISTS`. `forum_slug = 'chienperdu'` **posé dans les colonnes de l'`INSERT` lui-même** (+ filet `UPDATE ... WHERE forum_slug IS NULL`) : le piège documenté en tête d'init.sql n'a pas été reproduit une 2e fois.

RÉSERVES : (1) le bloc `catalog` de `community-types.js` **duplique les littéraux d'init.sql** (nom, description, hint) et **n'est lu par aucun code** — rien ne détectera une divergence si la description est modifiée en base sans toucher au module ; init.sql fait foi, c'est commenté comme tel ; (2) `chien-perdu` porte **le même sous-titre** que `chat-perdu` (« Signalement communautaire par commune ») — deux cartes voisines dans la catégorie Communauté avec la même ligne de fonction, à différencier si cela gêne ; (3) la dette « vue liste » du fil Chat perdu reste ouverte et concerne désormais **les deux** types.

## Fil "signaler en un clic — carte communautaire" (15/08/2026, DÉPLOYÉ)

Correctif mineur, front seul (aucune route/schéma touché). Le bouton « Signaler » ouvrait la 5ᵉ face sur la liste, formulaire de création caché derrière `.community-report-toggle` → il fallait 2 clics pour signaler.

**`site.js`** : `loadCommunityReports()` renvoie désormais `rows` (consommé par l'appelant). Transition liste→formulaire extraite dans `revealCommunityForm(card)` (même triplette `form.hidden=false / toggleBtn.hidden=true / list.hidden=true` déjà utilisée par `.community-report-toggle`, aucun nouveau mécanisme). Handler `.community-config` : après résolution du fetch, ouvre directement le formulaire si le profil n'a **aucun** signalement actif de ce type ; sinon ne fait rien (le formulaire n'est pas soumettable, 409 rate-limit « 1 actif/auteur »).

**Signal réutilisé sans nouvel appel réseau** : `rows.some(r => r.is_author)` — le signalement de l'auteur est TOUJOURS dans la réponse (auto-abonné dès la création), donc ce test suffit à savoir si le profil a déjà un actif.

**Bug évité en cours d'implémentation** : masquer `.community-report-toggle` seulement à l'ouverture aurait laissé le bouton caché indéfiniment après un « Je l'ai retrouvé » (qui recharge la liste par un autre chemin que `.community-config`). Corrigé en recalculant `toggleBtn.hidden` à **chaque** rechargement de `loadCommunityReports` (ouverture, spot, resolve, submit), pas seulement au clic sur « Signaler » — et seulement si le formulaire est fermé, pour ne jamais couper une saisie en cours.

Générique par construction (`.community-config`/`communityTypeOf`, hérité de la généralisation du 15/08) : testé sur `chat-perdu` et `chien-perdu`, aucun code par type.

**AUDIT DE SUIVI (15/08/2026) — 3 correctifs, même famille de bug** : après ce correctif, audit large du système communautaire à la recherche d'autres états d'affichage non recalculés. 3 confirmés et corrigés, un seul re-touché (aucune route/schéma) :
1. Le correctif "un clic" ci-dessus avait lui-même réintroduit le bug qu'il venait de fixer : dans `submitCommunityReport()`, le `setTimeout` qui referme le formulaire après une création réussie réaffichait `.community-report-toggle` **en dur**, sans revérifier `is_author` — reproductible uniquement sur une **création** (pas une adhésion, où `is_author` reste `false`). Le recalcul (`formEl.hidden` comme garde-fou) est extrait dans `syncCommunityToggle(card, rows)`, seul point de calcul, appelé par `loadCommunityReports()` ET, désormais, explicitement dans le `setTimeout` **après** la fermeture du formulaire (les `rows` du fetch déjà lancé sont capturées en closure — pas de requête dupliquée).
2. Vue liste : ouvrir le volet d'une carte communautaire (`list-view.js` `openParams()`) ne rafraîchissait jamais les signalements — contrairement à la vue cartes qui refetch à CHAQUE ouverture de la 5ᵉ face (conception explicite du fil Chat-perdu). Le volet affichait l'instantané figé au chargement initial de la page. Fix : `loadCommunityReports` exposée en lecture par `site.js` via `window.LBACommunity.load` (même fonction, pas de duplication), appelée dans `openParams()` quand `.card-community-face` est détectée — miroir exact du handler `.community-config`.
3. `unparkCard()` ne restaurait que les classes de flip (`face-task` etc.), jamais l'état interne du sous-formulaire (`.community-form.hidden`/`.community-report-toggle.hidden`/`.community-list.hidden`) — un formulaire ouvert pendant le parking en vue liste restait gravé sur le nœud DOM réel de la carte après fermeture de la ligne. Fix : remise à l'état fermé neutre (sans recalcul `is_author`, inutile — la prochaine ouverture le refait via le correctif 2 ou l'eager load).

Testé : les 2 comportements du fil précédent (un clic, masquage si actif) non régressés ; chemin CRÉATION réussie (pas seulement adhésion) ; cycle vue liste ouvrir/créer/fermer/rouvrir ; chat-perdu ET chien-perdu.

## Fil "compteur N discussions au verso" (14/08/2026, DÉPLOYÉ)

Le verso d'une carte/deck affichait « On en parle au forum → » sans jamais dire **combien**. Dette soldée.

**DÉCOUVERTE — DEUX ROUTES MORTES** : `/api/forum/source/:slug/count` et `/api/forum/deck/:slug/count`, posées le 30/07, n'étaient **câblées nulle part**. Elles ont été **laissées intactes mais NON utilisées** : les brancher aurait imposé **un fetch par carte** sur une grille de ~270 cartes. Décision : le compte descend par les charges qui existent déjà.

**IMPLÉMENTATION** : sous-requête `(SELECT COUNT(*) FROM forum_topics ft WHERE ft.hidden = false AND ft.source_id = s.id)::int AS topic_count`, ajoutée aux **4 charges qui exposaient déjà `forum_slug`** — `/api/sources`, `/api/favorites` (doublon assumé du SELECT de `/api/sources`, aligné), `/api/collections/:slug`, `/api/forum/u/:pseudo/decks` (même doublon côté decks, aligné). `hidden = false` : périmètre strictement public, **identique à celui du badge `@slug` juste à côté** — jamais un compte qui laisserait deviner du contenu masqué (même doctrine que /le-point et /u/:pseudo).

**EMPLACEMENT** : le compte est rendu **sur le lien de bas de verso** (« On en parle au forum (N) → »), **pas dans l'ancre du badge `@slug`** — ce badge est en `position:absolute` et un compteur y aurait cassé sa contrainte de placement. Rendu **strictement > 0** (`forumLinkText(n)`, une seule implémentation exportée par `LBACards`, consommée aussi par `deck-stack.js` : même règle « rien à 0 » pour les cartes et les decks).

⚠️ **PIÈGE DÉCOUVERT — `LBADeckStack.html()` ne reçoit PAS l'objet deck entier**, mais une liste de champs **énumérés explicitement** par chaque appelant. Un champ ajouté au payload serveur n'arrive donc PAS jusqu'au verso deck tant qu'il n'est pas ajouté à CHAQUE appelant : `topic_count` a dû être passé dans les **2 appelants qui rendent un verso de deck** (`site.js` pour le kiosque, `profile-decks.js` pour /u/:pseudo). À vérifier pour tout futur champ de verso deck.

RÉSERVE : une seule discussion existait en base au moment du test — le chemin `> 0` n'a été prouvé que sur une carte, et **« (2) » n'a jamais été vu à l'écran**.

## Fil "audit forum_slug préventif" (14/08/2026 ; LIVRÉ, aucune donnée touchée)

Solde la dette (3) du fil Chat perdu. **CORRECTION DE PRÉMISSE EN COURS DE ROUTE**, à retenir plus que le script lui-même : on soupçonnait `scripts/backfill-forum-slug.js` d'avoir un angle mort (sources virtuelles / insérées à la main). **Faux** — son `WHERE forum_slug IS NULL`, sans aucun filtre de type ni de module, couvrait déjà `chat-perdu`. Le vrai trou n'était pas dans la couverture du script mais dans son **statut** : un **one-off jetable jamais rejoué depuis le 30/07**. Un outil correct qu'on n'exécute plus ne protège de rien.

**AUDIT EN BASE (lecture seule)** : **291/291 sources** et **10/10 decks** ont un `forum_slug`, **0 collision**. `chat-perdu` était le seul cas, déjà réparé à la main. Rien à migrer.

**LIVRÉ** : `scripts/audit-forum-slug.js`, **lecture seule stricte** (aucune écriture, jamais). Il distingue explicitement **install neuve** (100 % NULL → ce n'est pas un oubli, c'est une base jamais backfillée) d'un **oubli ponctuel** (liste id / type / slug qui SERAIT calculé, avec la MÊME règle que `forum-slug.js` — jamais une variante). Filet de collisions. **Termine par les commandes à copier** (`--check` puis la commande réelle), parce que le vrai geste correctif est « relancer le backfill », pas « corriger à la main ». Codes de sortie 0/1/2. Miroir `backfill-forum-slug-decks.js`.

**DOC PLACÉE DANS `init.sql`, PAS ICI** (choix délibéré) : bloc `forum_slug` en tête du fichier (~l. 4516-4538), au plus près du geste dangereux — « ⚠️ TOUTE SOURCE AJOUTÉE ICI REPART SANS SLUG ». Une règle lue au moment d'écrire l'`INSERT` vaut mieux qu'une règle dans un document d'état.

## Fil "combobox filtrable — tag source/deck en création de sujet forum" (14/08/2026, LIVRÉ)

Le `<select>` natif de `/forum/nouveau` listait **~275 options à plat**, non filtrables. Remplacé par un combobox filtrable maison (`bindTagCombo()`, `public/js/forum.js`).

**DÉCISION D'ARCHI — MÊME VOCABULAIRE, CLASSES ET HANDLERS DISTINCTS.** Le composant reprend à l'identique l'ARIA et le clavier de `dynamic-enum` (`role=combobox`, `aria-expanded/controls/activedescendant/autocomplete/haspopup`, `role=listbox/option`, zone `aria-live`, ↓↑/Entrée/Échap), **mais avec des classes propres (`.tag-*`) et des handlers réimplémentés localement** — surtout PAS `.dyn-search`/`.dyn-enum`/`.dyn-option`, dont les handlers sont **délégués sur `document` dans site.js** : les emprunter aurait déclenché la machinerie du kiosque sur une page forum.

**FILTRAGE 100 % CLIENT** sur la liste déjà reçue (`tagMatch()`, modèle `suggestMatch()` de decks.js) — contrairement à `dynamic-enum` qui fait un lookup serveur par frappe. **Zéro appel réseau par frappe**, vérifié par test.

**REPLI DE NORMALISATION `normTag()`** : utilise `LBACards.normalizeSearch` si présente, **sinon un repli local de 3 lignes (minuscules + NFD)**, commenté pour dire pourquoi il existe — `cards.js` n'est pas chargé sur les pages forum à ce jour. Sans ce repli, « gemin » n'aurait pas trouvé « Géminides ».

**CONTRAT SERVEUR INCHANGÉ AU CARACTÈRE PRÈS** : le champ visible n'est pas soumis ; un input caché conserve `source:<id>` / `deck:<id>`. Re-frappe après sélection = invalidation (retour en recherche), même règle que `dynamic-enum`.

TESTÉ (Playwright, navigateur réel) : filtrage avec accents, pré-remplissage `?source=`, contrat ARIA complet, 0 appel réseau par frappe.

## Fil "pagination des sujets — profil, source, deck" (14/08/2026, LIVRÉ)

**LA DETTE ÉTAIT MAL NOMMÉE.** Notée « pagination de la page profil », elle s'est révélée être une **troncature silencieuse à TROIS endroits** partageant le même défaut — `LIMIT 30` sans `OFFSET` ni pager : `/u/:pseudo`, `/forum/source/:slug`, `/forum/deck/:slug`. Au-delà de 30 sujets, le contenu **disparaissait sans que rien ne l'indique**. Réflexe : quand une dette pointe UN écran, chercher le même motif sur ses frères.

Les trois routes sont alignées sur `/forum/c/:category` (**la référence, non touchée**), motif réutilisé tel quel (`page_`, `TOPICS_PER_PAGE`, `pagerHTML`). **Pas de `COUNT(*)` total ajouté** (le pager n'en a pas besoin, et c'est une requête de plus par page).

- **Base de pagination = le paramètre REÇU** (slug public ou id tel qu'il est arrivé), pas l'id canonique résolu : sinon un lien « Suivants » changerait l'URL sous les pieds du visiteur.
- Section « Ses decks » de `/u/:pseudo` rendue **en page 1 uniquement** (c'est un en-tête de profil, pas du contenu paginé).

⚠️ **BUG TROUVÉ HORS PÉRIMÈTRE, CORRIGÉ** : `#profile-empty` (« aucun contenu public ») se déclenchait sur `hasTopics` seul → une page 2 au-delà de la fin aurait affiché **« aucun contenu public » pour un membre actif**. État vide désormais borné à la page 1.

RÉSERVE : faute de volume réel, le cas « Suivants → » (n ≥ 30) n'a **jamais été observé en conditions réelles**.

## Fil "tag @slug rédactionnel dans le forum" (14/08/2026, LIVRÉ)

Premier cadrage réel d'un chantier jamais construit : mentionner une source, un deck ou un membre **dans le corps d'un message**.

**PÉRIMÈTRE** : syntaxe `@slug` **nue** (aucun préfixe de type), résolue **AU RENDU, jamais à la saisie** — un `@slug` écrit à la main reste valide, et un objet renommé ne casse pas les messages passés. Ordre de résolution **pseudo → source → deck** (0 collision mesurée entre les trois espaces).

**PÉRIMÈTRE DES PSEUDOS SUGGÉRABLES — STRICTEMENT LE DÉJÀ-PUBLIC** : auteur d'un sujet/message non masqué, OU propriétaire d'un deck public. **2 comptes sur 7** aujourd'hui. Jamais un annuaire des comptes — même doctrine que `/u/:pseudo` et `/le-point`. La réponse ne contient que `slug` + `name`.

**REFACTOR — BASE COMBOBOX PARTAGÉE** : `public/js/combo.js` (`LBACombo.create()`) extrait tout ce qui était commun (ARIA, clavier, rendu DOM, clic extérieur, `aria-live`) ; `bindTagCombo` (fil précédent) en devient **le premier client**, ne gardant que ce qui lui est propre. **Zéro régression prouvée, pas supposée : les 15 assertions Playwright du fil précédent ont été REJOUÉES après le remaniement.**

**RENDU** : `linkifyMentions()` appelée dans `bodyToHtml()` **APRÈS `escHtml()`, jamais avant** (zone XSS balisée). Résolution **EN LOT** : les slugs de toute la page sont collectés puis résolus en **3 requêtes max** (`= ANY($1)` par table), **jamais une requête par mention**. Le `href` est **toujours reconstruit depuis une valeur relue en base**, jamais du texte utilisateur réinjecté. Nouvel endpoint `GET /api/forum/mentionables` → `{ members, sources, decks }`, **un seul chargement par page**, partagé par les deux textareas.

**STYLE** : `.forum-mention` distinct de `.forum-slug-badge` — la pastille identifie un OBJET, une mention est du texte courant ; une pastille en plein paragraphe hacherait la lecture. `ugc.js` / `ALLOWED_FORUM_BODY` **non touchés** (déjà compatibles, vérifié).

TESTÉ : résolution des 3 types, texte brut laissé intact si aucune correspondance, détection fine (`contact@ecowatt` **non** transformé — pas de mention au milieu d'un mot ; `(@indiceuv)` reconnue ; point final hors du lien), XSS neutralisé, insertion au caret en fin ET au milieu du texte.

RÉSERVE NON TRAITÉE : la liste de suggestions est chargée **en tâche de fond** — taper `@xxx` dans la toute première seconde peut afficher « aucune correspondance » **à tort**. Sans gravité (la résolution est au rendu, pas à la saisie), corrigeable par un état « chargement… ».

## Fil "forum maison & profils publics" (30/07/2026, déployé sauf mention)

FORUM MAISON (fait maison — Discourse/Flarum écartés à l'exploration) : forum minimal rendu côté serveur (server/routes/forum.js), même ADN que le reste du site (HTML template strings, CSP script-src 'self' → zéro JS inline, escHtml systématique sur tout pseudo/titre/corps rendu).
- Schéma (init.sql, ALTER/CREATE idempotents) : forum_topics, forum_posts, forum_reports, subscribers.is_admin. Sujet rattachable à UNE cible exclusive : forum_topics.source_id XOR deck_id (CHECK forum_topics_one_target_chk + index uniques par table, namespaces séparés).
- Modération légère : signalements (forum_reports), masquage (hidden), verrouillage (locked), rôle admin (is_admin). Validation UGC (server/ugc.js), notification email à la réponse (mailer).
- Routes : /forum (catégories + sujets récents), /forum/c/:category, /forum/source/:slug, /forum/deck/:slug, /forum/t/:slug, /forum/nouveau, POST création/réponse/signalement + admin, /api/forum/taggables. Refonte visuelle complète (coquille forumShell native, breadcrumb, cartes-sujet, CTA .empty-btn, badges violets @slug).

IDENTIFIANTS PUBLICS STABLES (générés UNE SEULE FOIS, JAMAIS régénérés au renommage — stabilité des liens ; distincts de l'id interne) :
- forum_slug sur sources ET collections (server/forum-slug.js : NFKD → strip accents → [a-z0-9] → cap 64 → repli slug(id) → suffixe collision). Génération runtime à la création/fork (retry sur violation unique 23505) + résolution slug-ou-id côté serveur. Scripts backfill NON exécutés (Hugo).
- @pseudo sur subscribers (server/pseudo.js, RÉUTILISE forum-slug.js — aucune logique de slug dupliquée) : dérivé du display_name à la 1re pose, idempotent (UPDATE WHERE pseudo IS NULL → sûr d'appeler après n'importe quel set), unique (index partiel). Branché sur TOUS les points de pose du display_name : profile-autofill.js (1re connexion OAuth) + routes/decks.js (POST Mon compte). Runtime = retry 23505 ; comptes existants = backfill scripts/backfill-pseudo.js (NON exécuté).

PROFILS PUBLICS /u/:pseudo (nouvelle page, server/routes/forum.js) : agrège UNIQUEMENT du contenu DÉJÀ public — collections publiques + sujets forum du membre. Résolution STRICTE par pseudo (aucun repli par id : un id subscriber est interne, jamais résolvable publiquement). JAMAIS email/abonnements/préférences géo/intérêts/points/rang/tokens. noindex, 404 si pseudo inconnu, page neutre si vide, PAS de fil d'ariane. Decks = VRAIES tuiles-deck (LBADeckStack, réutilisation stricte de la grille /favoris `<div class="grid">` + bouton `.more-btn` + `.hidden-more` si >4) via public/js/profile-decks.js (nouveau) + endpoint GET /api/forum/u/:pseudo/decks (même forme que /api/collections, aucun champ privé, pas de Top20/adopted).

LIENS CROISÉS CARTE/DECK ↔ FORUM (cards.js, deck-stack.js) :
- Badge @forum_slug au verso, haut-gauche en miroir de flip-back (classe PARTAGÉE .card-forum-badge cartes+decks) → liste des sujets tagués (/forum/source|deck/:slug). Rendu seulement si forum_slug.
- Lien « On en parle au forum → » (ex-« Discussions sur le forum → ») sur les deux versos.
- Lien « Plus d'infos → » (.back-statut vers page statut) RETIRÉ des deux versos : le badge @slug tient désormais ce rôle « en savoir plus ».
- Auteurs cliquables partout : byline @pseudo → /u/:pseudo (forum + verso deck). Repli « Membre » non cliquable si pseudo NULL.
- Badge @slug affiché SUR un sujet forum → mène à la FICHE de l'objet (source /source/:id/statut ; deck officiel /collection/:id, deck perso public /deck/:token), plus à la liste de sujets (redondant : on est déjà dans le forum).

APPRENTISSAGES / PIÈGES (au-delà du quoi) :
- Identifiant PUBLIC = généré une seule fois, jamais régénéré au renommage (sinon liens morts), et TOUJOURS séparé de l'id interne (ne jamais rendre un id subscriber résolvable publiquement).
- Profil public = agrégation de données DÉJÀ publiques SEULEMENT ; points/rang/Top20 volontairement ignorés (même prudence que /le-point : rang privé).
- Réutilisation stricte > variante : les écarts (grille custom .profile-decks-grid, carte simple .forum-cat-card) ont dû être corrigés en cours de fil vers les composants réels (grille .grid favoris, tuile LBADeckStack). Réflexe : réutiliser .grid / .more-btn / .hidden-more / .forum-slug-badge existants.
- PIÈGE regex (deck-stack.js) : le verso deck remplace le .card-back de la carte d'aperçu par une regex LITTÉRALE `<div class="card-face card-back">` → NE PAS ajouter de classe sur ce nœud (sinon remplacement cassé silencieusement). Réserver l'espace du badge via le padding-top existant de .card-long-desc, pas via une classe sur .card-back.
- Ancres imbriquées = HTML invalide : topicCard = <article> avec titre-lien + badges FRÈRES ; liens de méta remontés au-dessus de l'overlay ::after via z-index (pattern .forum-slug-badge), jamais <a> dans <a>.

EN ATTENTE (Hugo, hors code) : node server/db/migrate.js (nouvelles tables/colonnes) ; backfills `node scripts/backfill-forum-slug.js` / `-decks.js` / `backfill-pseudo.js` (--check puis réel) ; is_admin=true sur le compte Hugo. README mis à jour ce fil (section « Le forum & les profils publics »).

## Fil "veilles paramétrées — YouTube, agenda ICS, météo des forêts, pollens" (27-30/07/2026 ; code commité, SQL DANS init.sql mais NON MIGRÉ — migrate/push/redeploy à faire par Hugo)

4 cartes touchées, UNE SEULE nouvelle (veille-agenda). Les 3 autres existaient déjà en base — cf. la convention « vérifier l'existant » ajoutée aux Conventions, née de ce fil.

**youtube-chaine** (existante, 153) — accepte désormais le PSEUDO public (@handle) et l'URL de chaîne (/@…, /channel/UC…, /c/…, /user/…) en plus de l'identifiant UC… historique, qui est invisible dans l'UI YouTube. Clé de paramètre INCHANGÉE (`channel_id`) → zéro migration d'abonnements. Résolution pseudo→identifiant par la page publique de la chaîne (lien `canonical` puis `"externalId"`, 2 motifs redondants), cache 30 j succès / 6 h échec (page à 2,3 Mo), safeFetchText + restriction d'hôte youtube.com. Fenêtre gardée à **24 h (pas 72 h)** : sur une carte vivante, élargir produirait une rafale rétroactive au 1er cycle. Doublon `veille-youtube.js` (commité par erreur dans 877be34) supprimé — commit isolé 986794a.
⚠️ **PIÈGE MAJEUR — MUR DE CONSENTEMENT UE** : depuis une IP européenne, youtube.com renvoie **302 vers consent.youtube.com**. safeFetchText interdisant le suivi de redirection (anti-SSRF), TOUTE résolution aurait échoué en prod (serveur en UE), **silencieusement et à 100 %** — sans crash, donc invisible. Contourné par le cookie `SOCS=CAI` (choix de consentement par défaut posé par le mur lui-même ; `CONSENT=YES+cb` ne marche PAS). Transposable à toute lecture de page Google/YouTube.
⚠️ **2e PIÈGE** : le paramètre `?user=` du flux et les URL `/user/…` désignent les ANCIENS noms d'utilisateur, PAS les pseudos actuels — `user=arte` renvoie une chaîne DIFFÉRENTE de `@arte`. Jamais l'utiliser comme raccourci de résolution.

**veille-agenda** (NOUVELLE, display_order **462**) — agenda personnel au format iCal (Google « adresse secrète », Outlook, Apple). Alerte sur les événements ENTRANT dans une fenêtre de préavis (1/2/3/7 j, défaut 1 j), chacun **une seule fois**. loadRef/dumpRef (pattern panneaupocket-veille) : clé = **UID + date d'occurrence** (un récurrent partage UN SEUL UID pour toutes ses dates — mémoriser l'UID seul aurait fait taire toutes les occurrences suivantes) ; amorçage SILENCIEUX au 1er cycle (sinon rafale à la souscription) ; purge des occurrences passées → référence bornée par la fenêtre.
⚠️ **DONNÉE SENSIBLE** : l'URL iCal donne accès en LECTURE à tout l'agenda → **jamais journalisée**, ni en succès ni en erreur ; les logs n'utilisent qu'une empreinte SHA-256 tronquée à 8 car. (garantie vérifiée par test, pas seulement affirmée).

**meteo-forets** (existante, 119) — SORT DE DORMANCE. L'ancien module devinait l'endpoint (`DPMeteoForets`), devinait le nom du champ de niveau (fonction `maxDanger()` qui scannait le JSON à l'aveugle) et exigeait `METEOFRANCE_FORETS_API_KEY`, jamais souscrite. Remplacé par le CSV public Météo-France (`meteofrance.s3.sbg.io.cloud.ovh.net/data/BULLETIN/MDF/mdf_<annee>.csv.gz`, jeu data.gouv « Archives de la Météo des forêts », **licence lov2**, bucket S3 sans robots.txt) : 25 Ko gz, **un seul fichier pour les 96 départements**, cadence quotidienne STRICTE vérifiée (63 bulletins / 63 jours, 0 trou, publication à 14:50 UTC). `niveau_j1` = **LENDEMAIN** du bulletin (pas « aujourd'hui »). Seuil 4 conservé (calibrage mesuré : niveau 4 = 1,3 % des observations ; ~4 épisodes/saison pour le dépt le plus exposé). `since` = horodatage du BULLETIN, pas l'heure de traitement (le TTL de cache ne doit pas influencer « depuis »). Garde-fou de fraîcheur 3 j : hors saison (juin→30 sept), le dernier bulletin n'est PAS rejoué — sinon la carte aurait alerté tout l'hiver. Repli d'année (mdf_2027 → 404 constaté).
⚠️ **ENUM OUVERT AUX 96 DÉPTS** — l'hypothèse « Sud uniquement » (15 dépts) était **empiriquement fausse** : 5 dépts ont connu un niveau 4 hors de cet enum (Haute-Garonne 4 épisodes, Deux-Sèvres, Meurthe-et-Moselle, Moselle, Essonne) tandis que 7 des 15 retenus n'en ont connu aucun — et le 29/07 les DEUX seuls dépts en niveau 4 étaient 54 et 57, tous deux hors enum.
⚠️ **LE FORMAT DU CSV CHANGE CHAQUE ANNÉE** (constaté sur les 3 millésimes) : 2024 séparateur VIRGULE + codes `1` + colonne `nom_dep` ; 2025 VIRGULE + `1` + **`dep_nom`** ; 2026 **POINT-VIRGULE** + `01` + `nom_dep`. Un parseur écrit contre 2026 se serait cassé en pleine saison. → séparateur détecté sur l'en-tête, colonnes indexées PAR NOM, codes normalisés (`1`→`01`, `2a`→`2A`), libellé jamais lu (on a le nôtre via geo.js). **Règle générale : sur un open data millésimé, ne jamais présumer la stabilité du format entre années.**

**pollens** (existante, 414) — paramètre **ESPÈCE** (`taxon` : tous + ambroisie/armoise/aulne/bouleau/graminées/olivier). **Coût réseau NUL** : les niveaux par espèce étaient déjà dans le CSV Atmo téléchargé, seule l'agrégation change (`levels` par taxon, pire cas par espèce indépendamment sur les communes ; `level`/`taxon` historiques strictement préservés → `qualite-air` non régressée, testée explicitement car le connecteur est partagé). Compat ascendante vérifiée sur 101 dépts : sans `taxon` == `taxon:'tous'`. Slugs SANS ACCENT (`graminees`) car stockés dans les params d'abonnement.

**NOUVELLE BRIQUE — server/sources/lib/ics-parser.js** : parseur iCalendar RFC 5545, **zéro dépendance** (aucune lib ICS dans le projet ; même esprit que lib/feed-parser.js pour RSS/Atom). Gère le dépliage des lignes repliées (obligatoire : 88 lignes repliées sur un flux Google réel), les échappements, les VALARM imbriqués (leurs champs ne sont PAS ceux du VEVENT), `STATUS:CANCELLED`, et 3 formes de DTSTART (VALUE=DATE / UTC `Z` / flottante-TZID lue en heure locale, approximation ASSUMÉE et bornée : la fenêtre se compte en jours). Périmètre RRULE et angle mort documentés en section Dette.

**PLATEFORME — server/params.js, plafond de longueur par descripteur** : `validateParams` tronquait TOUTE valeur string à 120 car. en dur. Une adresse iCal Google réelle fait **137 car.** → elle était tronquée EN SILENCE, passait quand même le `pattern` (`{1,300}` matche une chaîne de 120) et l'abonnement restait **muet à vie sans le moindre message**. Ajout d'un `maxLength` optionnel par descripteur (défaut inchangé à 120, plafonné à 500) ; `veille-agenda` déclare 300. Non-régression vérifiée (veille-rss tronque toujours à 120). ⚠️ **`veille-rss` porte la même faiblesse latente** pour une URL de flux > 120 car. — non traité, décision à prendre.

**FRONT — pré-remplissage géo sur cartes à 2+ champs** : régression introduite par le paramètre `taxon`, corrigée. Détail en section « Fonctionnalités produit ». Reproduite et validée en **navigateur réel (Playwright/Chromium, déjà en dépendance)** avec comparaison AVANT (`git show HEAD:`) / APRÈS sur 5 cartes — méthode réutilisable pour tout correctif front.

**LIBELLÉS — « dans le {département} » est FAUX** : correct pour 15 dépts du Sud, faux dès l'ouverture (« dans **le** Moselle », « dans **le** Gironde »). Les articles des départements français sont trop irréguliers pour une règle (« en Moselle », « dans l'Aisne », « dans les Bouches-du-Rhône », « à Paris ») → **département EN TÊTE, sans article** (`🔥 Meurthe-et-Moselle : danger…`) plutôt que 96 exceptions codées. Appliqué à meteo-forets et pollens. ⚠️ **`qualite-air.js` porte encore le défaut** (101 dépts en prod) — correctif isolé à faire dans un commit dédié (décision Hugo 29/07).

**PROSPECTION « risques saisonniers » (29/07)** — 5 pistes explorées, 2 retenues (météo des forêts, pollens ci-dessus), 3 écartées : **AlerteCyber** (doublon démontré de `cert-fr-alertes` déjà en prod : mêmes failles WordPress/SharePoint avec **3 à 8 j de retard**, aucun flux structuré, `lastmod` du sitemap inutilisable car pollués par une migration en masse) ; **Pollinariums Sentinelles** (web.alertepollens.org = site vitrine, alertes par e-mail aux inscrits, aucun état public — les « 9 espèces » relèvent de ce réseau, PAS du référentiel Atmo qui en compte exactement 6) ; **Alerte Enlèvement** (site mono-page, toutes les URL retombent sur l'accueil, aucun état « alerte en cours » à surveiller — et le dispositif est PAR CONSTRUCTION une diffusion massive TV/radio/FR-Alert : une carte opt-in serait plus lente et moins couvrante, contraire au positionnement « le signal que personne d'autre ne pousse »). Eaux de baignade : reportée, cf. section Dette.

**rappel-conso / catégorie automobile (27/07)** — VÉRIFICATION SEULE, aucun code changé : la catégorie « automobiles et moyens de déplacement » était déjà présente et strictement conforme aux 10 facettes de l'API (match exact, accents compris). Le gap `GRAVE` découvert à cette occasion est en section Dette.

## Fil "header — icônes, badge & menu unique" (03/08/2026, non déployé — diffs en revue)

Fil entièrement front (`public/js/header.js`, `session.js`, `mobile-header.js`, `site.js`,
`view-mode.js`, `index.html`, `css/site.css`) + `docs/header-spec.md` mis à jour au fil de l'eau.

**FAMILLE D'ICÔNES `.hd-link` / `.hd-ic` (nav-right)** — `nav-right` desktop = `🛍 boutique` ·
`🔔 mes alertes` · `♥ favoris` · `Se connecter | avatar`. Grammaire commune : **aucune couleur
au repos** (trait `--muted`), le violet `--amber` **monte du bas vers le haut au survol**
(deux calques SVG superposés, `.hd-ic-base` au trait + `.hd-ic-fill` révélé par un
`clip-path: inset()` qui remonte) ; padding horizontal 5px. Animations propres : la **cloche
penche** (`hd-bell-ring`), le **contour du cœur déborde en s'estompant** (`hd-heart-echo`,
masque SVG *stroke only* — même vocabulaire que le halo `ping` du KPI), l'**avatar se
retourne** sur une roue de paramètres. Le **sac boutique** est en `strokeOnly` : seul le
dessin se colore, l'intérieur ne se remplit pas. `prefers-reduced-motion` neutralise tout.
Markup = SOURCE UNIQUE dans header.js (`BELL_HTML`/`HEART_HTML`/`BAG_HTML`, exposés via
`window.LBAHeader.ICONS`) + **copie miroir statique dans index.html** (la home garde son
header) — commentaire de rappel des deux côtés, toute retouche doit être répercutée.
Supprimé au passage : l'état « cœur rempli en permanence » quand des favoris existent
(`markFavHeart` + `.fav-link.has-fav`).

**« Mon compte » = AVATAR À INITIALE** (session.js) : 1re lettre du pseudo, sinon de l'email,
dans un cercle qui se remplit de violet puis se retourne sur une roue. Initiale **mise en
cache local** (`lba-initial`) pour que les pages appelant `renderHeader(null)` affichent la
bonne lettre sans clignotement ; `LBASession.setAvatarInitial(name)` exposé et appelé par
`fillAccountHeader` (le pseudo prime sur l'email). État anonyme = pilule texte « Se connecter »
inchangée (l'avatar est démonté).

**KPI DU HEADER = PASTILLE DE NOTIFICATION** : `mobile-header.js` dépose le `.kpi` **dans le
lien `.mine-link`** (et non plus en fin de `.nav-right`) ; le CSS en fait un rond vert chiffré
superposé à la cloche. Halo `ping` conservé ; `#kpi-dot`, `#kpi-text` et l'overlay qui
s'étirait au survol disparaissent ; `data-active != "true"` (0 alerte) → aucune pastille.
Le `.kpi` du hero garde sa pilule. **BUG CORRIGÉ au passage** : le compteur ne partageait pas
le prédicat de la vue « Mes alertes » — une alerte **en pause** quittait la liste mais
continuait d'être comptée (pastille « 2 » sur une liste vide, persistant au rechargement
puisque le serveur renvoie `muted: true`). Les deux calculs (`refreshMineDependent` et le
calcul initial) utilisent désormais `mineHidden` / excluent `muted`.

**MENU UNIQUE POUR TOUTE LA NAVIGATION (changement de politique)** : la règle « pages
secondaires = 1re ligne seule » est ABANDONNÉE. En desktop, `header.js` injecte désormais
aussi les **rangées 2 et 3** (`buildCatRail`) : mêmes puces, mêmes comptes, même tri
(fréquence puis libellé) que le kiosque, calculés depuis `/api/sources` + la taxonomie
(`LBACat` si déjà chargé, sinon `/api/categories` — aucune page n'a besoin d'inclure
`categories.js`). Les `order` CSS existants (`.toolbar` order 6, `.chips-secondary-wrap`
order 7 sous `body.m-kiosk`) placent les rangées sans CSS nouveau. Deux différences
inévitables faute de grille : le clic **navigue** (`/?cat=…`, `/?mode=nouveautes|selection`,
`/` pour « Toutes ») au lieu de filtrer sur place, et **aucune puce n'est marquée active**.
**EXCEPTION : l'espace développeurs** (`proposer.html`, `body.dev`) garde ses règles — 1re
ligne seule, thème sombre (`buildCatRail` sort immédiatement). **MOBILE INCHANGÉ** : le rail
est masqué sur `body.m-secondary.m-kiosk` ≤720px, les catégories restent dans le menu plein
écran. La home garde toujours son header propre — **dette assumée NON levée**, mais le rendu
est désormais identique. ⚠️ Ces pages font un appel `/api/sources` supplémentaire au
chargement (même endpoint public que la home, cacheable) — restreignable à une liste de
pages si le coût gêne sur les pages légères.

**RETRAITS DU HEADER** (simplification) : bouton **thème** `.theme-btn` retiré (markup + CSS +
liaisons) — `window.toggleTheme` reste exposé mais **n'a plus aucun déclencheur dans l'UI**,
et le réglage « Mon compte › Apparence » annoncé par la spec n'existe pas dans `profile.js` :
**à recâbler**. Bouton **`#view-toggle` cartes/liste SUPPRIMÉ** (markup, styles, `bindToolbarButton`,
relocalisation) — le réglage passe uniquement par « Mon compte › Apparence », qui appelle déjà
`LBAViewMode.set` (`appearance.js`) ; `view-mode.js` conserve état, persistance et
`lba-view-change`. Bouton **« Déposer une alerte » : rotation de libellé supprimée** (plus
d'alternance avec « Ajouter un deck »), libellé fixe → `/proposer`. ⚠️ Les keyframes
`bd-slide-in` sont **réutilisées** par la rotation du H1 de `/favoris` (`favoris-title.js`) —
conservées avec un avertissement, ne pas les supprimer avec le reste.

**MISE EN PAGE DU HEADER DESKTOP** — les écarts étaient imprévisibles à cause du
`justify-content: space-between` de `.nav` : la recherche étant plafonnée, l'excédent se
répartissait dans TOUS les interstices, rattrapable seulement à coups de marges négatives.
Bloc ≥1024px passé en **`flex-start` + `margin-left: auto` sur `.nav-right`** → écarts
déterministes (logo → « Déposer » = 26px + gap ; « Déposer » → recherche = 10px de gap seul).
Le bouton « Déposer » : le **`+` est ancré à gauche** (`justify-content: flex-start` +
`.bd-label { flex:1; text-align:center }`) donc immobile d'un libellé à l'autre, et se réduit
au **« + » seul entre 722 et 1299px** (libellé dans le `title`). Séparateur **« · » entre les
puces** du header (`::before` absolu dans le `gap`, hors zone cliquable).

## Fil "correctifs sécurité — audit statique Passe 1" (14/08/2026, DÉPLOYÉ par Hugo)

Audit STATIQUE du code source (lecture seule, distinct des 4 robots nocturnes qui, eux, sondent la prod). Passe 1 = sécurité. Trois correctifs appliqués (rien de bloquant remonté : pas d'injection SQL, pas de secret de clé en dur, CSP `script-src 'self'` solide, validation UGC stricte) :
- **`author_subscriber_id` retiré de la SORTIE de community-reports.js.** Il fuyait au client via `REPORT_COLUMNS` (GET) ET via le `RETURNING` du POST create/join, alors que le booléen `is_author` — calculé séparément par `(r.author_subscriber_id = $1) AS is_author` — couvrait déjà le besoin front. Même principe que `collections.js` (`delete r.owner_subscriber_id`, jamais exposé), non respecté ici jusqu'à ce correctif. Retrait fait **au niveau du SELECT lui-même** (colonne ôtée de REPORT_COLUMNS + du RETURNING), pas un `delete` post-requête, après vérif qu'aucun code serveur ne consomme cette colonne via REPORT_COLUMNS (le filtre GET lit `subscribed/lat/lon/radius_km` ; les chemins create/join ne relisent jamais l'id). `is_author` intact (lit la colonne de table directement, hors REPORT_COLUMNS) ; les requêtes internes qui ont besoin de l'auteur (notif spot, `WHERE author_subscriber_id=$2` de spot/extend/resolve) sont distinctes et conservées. **Zéro régression confirmée** : grep front = 0 usage de `author_subscriber_id`, uniquement `is_author` (cards.js/site.js) ; la réponse create/join n'est lue que pour `commune_nom`.
- **`IP_HASH_SALT` : fallback constant en dur (`'lba-ugc-report-salt'`) supprimé de ugc.js.** Le serveur **échoue désormais bruyamment au démarrage** (`throw` + message explicite « IP_HASH_SALT manquant… ») si la variable est absente, plus de hachage silencieusement faible (un sel public rendrait les hash IPv4 réversibles par table arc-en-ciel). Sel fort généré hors repo, posé sur Railway par Hugo. Portée vérifiée : `IP_SALT`/`IP_HASH_SALT` n'existe QUE dans ugc.js (un seul usage, `hashIp`). ugc.js étant requis par la chaîne d'init (profile-autofill, forum, decks, user-tasks, community-reports), le throw bloque le boot serveur. **Effet confirmé (pas supposé) sur les hash déjà stockés** : changer le sel invalide les correspondances des `reporter_ip_hash` de l'anti-doublon signalement forum `(post_id, ip_hash)` → **réinitialisation ponctuelle unique** de cet anti-abus (un couple (post, IP) déjà signalé pourra l'être une fois de plus), **aucune perte de données** (hachage à sens unique), auto-cicatrisant. `community_reports` **non concerné** (n'appelle pas `hashIp`).
- **`linkifyMentions` (forum.js) durci** : `escHtml()` appliqué à `hit.href` (`href="${escHtml(hit.href)}"`). Vecteur jugé faible (ids DB internes, charset contraint, slug capturé sur du HTML déjà échappé) mais coût nul → défense en profondeur.
- ⚠️ **À GARDER VISIBLE** : `IP_HASH_SALT` doit aussi être ajouté au **`.env` local de dev** — posé sur Railway uniquement à la date du correctif. Tout process local chargeant ugc.js (serveur, scripts) l'exige désormais, sinon throw au démarrage. `.env.example` **non modifié** (à documenter si souhaité).
- **Notes mineures Passe 1 NON corrigées (reste-à-faire, non urgent)** : (a) pas de rate-limiter dédié sur `POST /forum/post/:id/report` — atténué par l'anti-doublon `(post_id, ip_hash)` existant ; (b) `forum_reports.reason` stocké brut (slicé à 40) — aucun rendu HTML identifié qui l'exposerait aujourd'hui, à re-vérifier si une vue admin l'affiche un jour.

## Fil "correctifs audit statique — Passe 2 (incohérences & bugs latents)" (15/08/2026, à déployer par Hugo)

Passe 2 de l'audit statique (distinct des robots nocturnes prod). Famille visée : état non recalculé, transition asymétrique X→Y sans Y→X, condition ne couvrant pas tous les chemins, doublons « à garder alignés » qui dérivent. **Aucune migration** (aucun schéma touché par les 3 correctifs).
- **Doublons de SELECT vérifiés SAINS** : `/api/sources` ↔ `/api/favorites` restés **alignés colonne par colonne** (17 colonnes identiques, dont `forum_slug` + `topic_count`, même `.map(withCommunityConfig)`). `/api/forum/u/:pseudo/decks` ↔ `/api/collections` restés alignés (omissions `Top20`/`adopted`/`adopt_count` **volontaires et documentées** — profil sans classement).
- **DÉRIVE TROUVÉE ET CORRIGÉE — `/api/collections/:slug`** : le fichier affirmait « items enrichies **exactement comme** GET /api/sources » mais le SELECT avait divergé (**`s.forum_slug` ET le sous-`SELECT … topic_count` manquants**). Conséquence réelle reproduite : sur `/collection/:id`, chaque carte perdait silencieusement son badge `@forum_slug` et son lien « On en parle au forum (N) → » (le verso de cards.js les construit à partir de ces deux champs). **Corrigé** : les deux ajoutés au SELECT items à l'identique de `/api/sources` (même sous-requête/alias). Purement additif, aucun consommateur ne dépendait de leur absence (grep collection-page.js = 0), pas de GROUP BY à ajuster.
- **ASYMÉTRIE LIKE/FAVORI CORRIGÉE (décision produit confirmée par Hugo) — `/api/favorites/sync`** : la sync (remontée du localStorage `lba-likes` à la connexion) n'alimentait que `favorites`, jamais `source_likes` → un cœur posé avant connexion devenait favori mais n'était **jamais compté** dans `likes_count`. Désormais la sync **crédite AUSSI `source_likes`** (INSERT `ON CONFLICT (source_id, subscriber_id) DO NOTHING`, même patron que POST /like) **et recalcule `likes_count = COUNT(source_likes)`** pour les seules sources réellement créditées (RETURNING), le tout **dans une transaction** (BEGIN/COMMIT/ROLLBACK). **Précision d'intention à garder écrite** : ceci **NE rouvre PAS le vote anonyme temps réel** fermé par le refactor `likes_count` du 14/08 — au moment de la sync, le `subscriber_id` est déjà **réel et authentifié** ; seuls les cœurs posés AVANT connexion sont **convertis après coup**, dédupliqués par la PK `source_likes`. Idempotent : une sync rejouée n'insère rien (`ON CONFLICT`) → `RETURNING` vide → aucun `UPDATE` → compteur intact.
- **BUG TROUVÉ ET CORRIGÉ (même famille que le bug communautaire — état d'affichage marqué depuis une source partielle) — cœur ≠ favori serveur sur `/collection/:id` et `/deck/:token`** : ces 2 pages marquaient les cœurs **uniquement** depuis `localStorage lba-likes`, sans croiser `serverFavs` (contrairement au kiosque). Un compte connecté ayant favorisé une source ailleurs (auto-abonnement, autre appareil) y voyait un **cœur vide à tort**. Le fetch était **déjà partagé** (`LBASession.fetchAlerts` → même `s.data.favorites` que le kiosque) ; seul le marquage visuel manquait. **Réutilisation stricte** : logique de marquage extraite dans **`window.LBALikes.markFavorites(root, favIds)`** (site.js), réutilisée par le `markLikes()` du kiosque (délègue, **zéro régression**) ET par les 2 pages (`favIds = s.data.favorites`), avec **repli localStorage-seul** si `LBALikes` absent ou visiteur anonyme (croisement `serverFavs` = comptes connectés uniquement). Clic (like/unlike) déjà délégué à site.js, inchangé.
- **init.sql — audit d'idempotence complet : RIEN de non-idempotent.** Tous les `ADD COLUMN`/index en `IF NOT EXISTS`, INSERT sources/states en `SELECT … WHERE NOT EXISTS`, migration de colonne `category` en `DO $$ … IF EXISTS`, **tous les `ADD CONSTRAINT`/CHECK en `DO $$ … IF NOT EXISTS (SELECT 1 FROM pg_constraint …)`**, contrainte UNIQUE `deck_reports` précédée d'un DELETE de dédoublonnage.
- **Orphelin identifié mais NON corrigé (volontaire, documenté dans le code)** : `DELETE /api/sources/:id/like` n'a plus d'appelant client (le ❤ retire le favori via `DELETE /api/favorites`) mais reste **l'inverse exact du POST**, conservé délibérément (commentaire en clair api.js).
- **Déploiement** : `git push` → `railway up`, **AUCUNE migration** (schéma non modifié par les 3 correctifs). **Requête de contrôle post-déploiement (Hugo)** : `SELECT source_id, COUNT(*) FROM source_likes GROUP BY 1 HAVING COUNT(*) <> (SELECT likes_count FROM sources WHERE id = source_id)` — doit renvoyer **0 ligne** (compteur cohérent avec la table après une sync).

## Fil "correctifs audit statique — Passes 3 & 4 (qualité/maintenabilité & performance)" (15/08/2026, à déployer par Hugo)

Passes 3 (qualité/maintenabilité) et 4 (performance) de l'audit statique (distinct des robots nocturnes prod). **Aucune migration** (aucun schéma touché). Clôture du fil d'audit large ouvert le 14/08.

**Passe 3 — qualité/maintenabilité : tout vérifié SAIN sauf un finding.**
- **Vérifié sain** : gestion d'erreur réseau/DB (best-effort `catch → null` sur IPLocate/geo.api.gouv.fr, Resend systématiquement `try/catch` + compteur d'échec, notifs post-réponse en `.then().catch()` détachées jamais awaitées) ; fonctions longues (`history` ~83 l., `runCycle` ~103 l.) linéaires et commentées, **signalées pas refactorisées** ; magic numbers déjà nommés (`EPISODE_THRESHOLD_MS`, `REF_MAX_BYTES`, `COMMUNES_TTL_MS`, `WINDOW_MS`) ; **aucun import inutilisé**.
- **DÉRIVE TROUVÉE ET CORRIGÉE — `enrichItems` (decks.js), même motif que C1 (Passe 2)** : le commentaire affirmait « Cartes enrichies d'un deck (**même forme que /api/sources**) » mais le SELECT **omettait `s.forum_slug` ET le sous-`SELECT … topic_count`**. Conséquence : cartes du **deck perso en édition (Mes decks)** sans badge `@forum_slug` ni compteur de discussions. **Découverte clé** : `/api/decks/shared/:token` (la route de la page publique **`/deck/:token`, réellement visitée**) **délègue à la MÊME fonction `enrichItems`** — il n'a pas de SELECT propre. **Un seul correctif a donc réparé les deux consommateurs** (édition ET page publique). Les deux colonnes ajoutées à l'identique de `/api/sources`/collections.js (même sous-requête/alias, `hidden = false`). Purement additif, zéro régression (les consommateurs via `LBACards.cardHTML` lisaient déjà ces champs, les ignoraient si absents).

**Passe 4 — performance : aucun problème avéré, une optimisation posée.**
- **Vérifié sain** : **aucun N+1 DB** (le poller boucle des envois RÉSEAU irréductibles — 1 push = 1 appel — pas des requêtes DB évitables ; `checkWithParams(combos)` = un seul appel ; refs chargées groupées en Map) ; **tous les index nécessaires présents** (croisé avec init.sql : `idx_subscriptions_source`, `idx_source_events_source_date`, `idx_forum_topics_source` partiel, `idx_favorites_sub`, `idx_community_reports_active` partiel, PK préfixe `source_id` pour le recalc `source_likes`) ; **aucun fetch client dupliqué** restant (`collection-page.js`/`deck-shared.js` mutualisent `LBASession.fetchAlerts`, déjà fait en C3).
- **POINT MESURÉ ET TRAITÉ — `/api/sources`** : la route retourne ~290 lignes et exécute **3 sous-`SELECT` corrélés par ligne** (`subscriber_count`, `last_activated_at`, `topic_count`) = ~870 exécutions/appel, **sans cache**. **Décision Hugo appliquée** : `Cache-Control: public, max-age=60` posé sur la réponse. Sûr car **aucune donnée privée/par-utilisateur** (confirmé par lecture : pas de `token`, pas d'`authenticate`, catalogue identique pour tous). **Choix conservateur `max-age` SANS `s-maxage`** : profite au navigateur ; côté edge Cloudflare (frontal confirmé, `index.js`), sans effet tant qu'aucune Cache Rule `/api/*` n'existe — **à confirmer par Hugo**. **Contrepartie de fraîcheur ~60 s assumée et documentée en commentaire** (un like/topic_count fraîchement changé peut mettre jusqu'à 60 s à apparaître ; acceptable vu le volume et le caractère non critique de ces compteurs).

**Dette structurelle notée (PAS codée)** : les **4 SELECT « forme /api/sources »** (`sources`, `favorites`, `collections`, `enrichItems`) restent des copies manuelles **sans garde de synchronisation** — les 2 drifts trouvés (C1 collections.js, celui-ci decks.js) le prouvent. Factorisation possible d'un fragment SQL partagé type **`SOURCE_CARD_COLUMNS`** (comme `REPORT_COLUMNS` existe déjà pour community-reports) — **à arbitrer dans un futur fil, hors périmètre ici**.

**Déploiement** : `git push` → `railway up`, **AUCUNE migration** (Passes 3+4 n'ont touché aucun schéma).

---

**CLÔTURE GLOBALE — audit statique large (Passes 1 à 4, ouvert le 14/08, clos le 15/08).** Bilan condensé : **sécurité SAINE** hors 2 correctifs appliqués (fuite d'id interne `author_subscriber_id`, sel IP `IP_HASH_SALT` en dur → fail-loud) ; **cohérence des données SAINE** hors 1 asymétrie produit corrigée (favoris/likes à la sync) ; **2 drifts de SELECT « documentés alignés »** trouvées et corrigées (collections.js en Passe 2, decks.js en Passe 3) ; **1 bug d'affichage généralisé** (cœur ≠ favori serveur, `serverFavs`) ; **init.sql intégralement idempotent** (audit complet) ; **performance SAINE** hors 1 optimisation de cache posée. Aucun 🔴 bloquant sur l'ensemble des 4 passes.

## Plateforme & infra

Express/PostgreSQL/Railway, poller */30 (runCycle ÉCRIT et notifie — JAMAIS depuis un agent), checked_at chaque passage, factories (vigilance/statuspage/calendar+upcoming/vacances/release/meteoalarm/insee-bdm), briques lib/ (feed-parser, prefectures, safe-fetch, ugc), geoip-lite local. Comptes : magic link + OAuth Google/GitHub (PAS d'autre fournisseur — décision actée ; Apple seulement si app iOS un jour), sessions 90j, suppression cascade. Notifs : Resend + push VAPID (PWA installable).
URL CANONIQUE = APEX (basculé ce fil) : labonnealerte.fr officielle, www redirige en 301 via Cloudflare Redirect Rule (query string PRÉSERVÉE — indispensable aux liens magiques ?token=). Toutes les refs en dur basculées (og:url server-rendered deck/collection/source, liens de partage JS, mailer PUBLIC_SITE+MYALERTS_URL, poller/webpush fallback, sources url par défaut, README, .env.example). BASE_URL Railway = apex ; consoles OAuth Google/GitHub mises à jour (redirect_uri apex, anciennes URI www retirées). DNS : enregistrement @ Cloudflare = CNAME Railway (connexion auto Railway), plus de A OVH 213.186.33.5. Sessions token en localStorage per-origin (lba-token) → les connectés sur www repassent par un magic link après bascule (désagrément mineur, session serveur 90j intacte).
EMAILS — identité expéditeur (mailer.js) : domaine d'envoi TECHNIQUE reste alert.labonnealerte.fr (reco Resend : isoler la réputation d'envoi sur un sous-domaine dédié — NE PAS basculer vers le domaine racine). Compensation cohérence : From = 'LaBonneAlerte <noreply@alert.labonnealerte.fr>' (constante FROM partagée) + tous les sujets préfixés « LaBonneAlerte · » (helper subject() partagé, 4 types : confirmation, lien magique, alerte, digest veille) ; suffixes redondants « à La Bonne Alerte » retirés des transactionnels (sujets < ~40 car., anti-troncature mobile).

## Robots (headless, robots/PLANIFICATEUR.md, développés)

R1 maintenance QUOTIDIEN 4h (checked_at/events/TODO/fragiles via scripts *-readonly.js) · R2 audit sécu HEBDO dim 3h · R4 veille sources HEBDO mer 3h · R3 soumissions DORMANT (activer au lancement ; sonde safeFetchJson tracée, 1 sonde/URL, plafond/run, ne touche jamais enabled). Garde-fous absolus : lecture seule, rapport only, jamais runCycle, validation humaine.
ROBOT 1 — EXTENSIONS À AJOUTER (issues du fil, RAPPORT ONLY) : (a) CONTRÔLE SCHÉMA attendu vs réel = comparer le schéma attendu (dérivé des ALTER TABLE d'init.sql + un noyau typé de colonnes) aux colonnes réelles en base (information_schema) → signale une migration oubliée à chaud (garde-fou « push ≠ déployé / migrate oublié » ; le contrôle post-migrate manuel le refait à chaud, Robot 1 la nuit). (b) VITALITÉ des cartes PanneauPocket curées (vague L + arrosage) = proxy last_activated_at (dernière alerte), SEUIL 90 j sans panneau → candidate à désactivation (RAPPORT seul, jamais d'action). (c) COMBOS ORPHELINS = lignes source_param_states dont la combinaison n'est plus souscrite (dont ref morte). (d) STABILITÉ des ids ?panneau= (cf. SURVEILLANCE PanneauPocket). ⚠️ À VÉRIFIER au 1er run : sémantique de comparaison jsonb (params) — utiliser IS NOT DISTINCT FROM plutôt que = (égalité jsonb sensible à l'ordre/typage).

## Conventions de travail (à respecter dans tout nouveau fil)

- Hugo : Claude Chat = architecture + prompts → Claude Code (VS Code, --dangerously-skip-permissions) exécute → rapports collés ici. Windows PowerShell 5 : PAS de && — git LIGNE PAR LIGNE (add . / status / commit / push), messages sans accents.
- Prompts de sources : explorer l'API réelle avant de coder ; dates vérifiées sur site officiel (JAMAIS de mémoire) ; non annoncé = TODO daté (JAMAIS de date inventée) ; écarter sans forcer en documentant ce qui manque ; ton factuel et calme sur les sujets anxiogènes ; anti-spam absolu (seuils stricts, jaune/mineur exclus).
- ⚠️ RÈGLE SQL (permanente, ne plus la répéter prompt par prompt) : **un diff SQL qui n'est pas dans server/db/init.sql n'existe pas**. Tout bloc SQL produit doit être COLLÉ dans le fichier, jamais laissé dans le rapport seul. Deux précédents coûteux : youtube-chaine est resté en prod sur l'ancien pattern `^UC…$` (refusant les @pseudo) et veille-agenda n'avait AUCUNE ligne en base alors que son code était déployé — dans les deux cas le SQL était « livré » dans un rapport. COROLLAIRE : init.sql porte SOUVENT DEUX emplacements de params_schema par carte (l'INSERT inline pour installation neuve ET l'UPDATE de migration) — aligner LES DEUX. Et tout contrôle de cohérence SQL↔JS doit ÉCHOUER s'il ne trouve aucun emplacement (un contrôle qui ne trouve rien ne doit jamais conclure au succès).
- Vérifier l'EXISTANT avant de créer une carte : 3 fois sur les dernières vagues, une carte « nouvelle » doublonnait une source déjà en base (youtube-chaine/veille-youtube, meteo-forets, pollens, cert-fr-alertes/AlerteCyber). Réflexe : `ls server/sources/ | grep -i <sujet>` + `grep -c "'<id>'" server/db/init.sql` AVANT toute conception.
- Séquence : add/status (.env et .claude/ absents !)/commit/push → node server/db/migrate.js dans le shell Railway si init.sql touché (⚠️ le .env local pointe la DB de PROD) → Deploy logs. Fusions : comptes de contrôle SQL zéro-perte. Deux dépôts Railway (doomname d'abord si touché).
- ⚠️ GitHub : shadow ban temporaire en cours (ticket ouvert) — déploiements Railway directs possibles ; resynchroniser GitHub à la levée, et revenir à « commit d'abord, déploiement ensuite ».
- ⚠️ PUSH ≠ DÉPLOYÉ (leçon du fil, 25/07) : après chaque push, DÉCLENCHER/VÉRIFIER le redeploy Railway (shadow-ban = pas de déploiement auto fiable ; symptôme réel vécu : colonnes migrées mais code non live → rien ne se peuplait). Après chaque migrate : CONTRÔLE SCHÉMA lecture seule à chaud (information_schema, cf. Robot 1 qui le refait la nuit). ORDRE pour un chantier à colonne nouvelle : **migrate → push → redeploy → vérif** (le code doit de toute façon tolérer l'absence de colonne via dégradation silencieuse). Vérifier au démarrage la ligne « [poller] N source(s) chargée(s) : … » (liste des modules réellement chargés dans le process courant). SÉQUENCE STRICTE confirmée (fil 27/07) : git push → `railway up` (JAMAIS présumer l'auto-déploiement actif) → attendre la FIN du build (logs complets, pas juste le début) → SEULEMENT ensuite node server/db/migrate.js en shell Railway → vérifier par une REQUÊTE DIRECTE contre la base (un scan de fichier local ne prouve que l'état local, jamais ce qui est réellement déployé ; corollaire du piège descriptions : lire le DETAIL: d'une erreur Postgres, pas grep/regex le fichier). Ces étapes (push/railway up/migrate) sont faites par Hugo — inutile de les lui proposer.
- display_order : utilisés jusqu'à ~365 ; toute nouvelle vague prend une plage au-dessus.
- ⚠️ RÈGLE PRODUIT (14/08/2026) — **SOUS-TITRE DE CARTE : 38 CARACTÈRES MAXIMUM**, quel que soit le type de carte (broadcast, paramétrée, communautaire, user-task). Le sous-titre (`sources.subtitle`, rendu en `.card-subtitle`) est la ligne de fonction sous le titre : au-delà de 38 car. il passe sur deux lignes et déséquilibre le recto au format 5/7. La règle est STRUCTURELLE, pas orale : CHECK `sources_subtitle_len` dans init.sql (idempotent, précédé d'une troncature de sécurité à 37+« … » pour qu'une ligne posée hors du fichier ne fasse jamais échouer la migration). Aucun sous-titre n'est saisi par un utilisateur (ni deck, ni signalement) → la contrainte DB est le point d'application COMPLET. Mise en conformité du 14/08 : 24 sous-titres réécrits (22 vus en base, 39 à 52 car. + 2 vus uniquement dans les INSERT inline — `jours-feries` et `grands-salons`, dont la valeur en base venait d'un UPDATE ultérieur : une install neuve aurait échoué au CHECK). PIÈGE CONFIRMÉ : un contrôle en base seule et un contrôle sur le fichier seul donnent des résultats DIFFÉRENTS — faire les DEUX.
- ⚠️ AUCUN SCRIPT INLINE POSSIBLE (constaté fil V3, 30/07) : la CSP d'index.js pose `'script-src': ["'self'"]`, helmet est monté AVANT les pagesRouter → tout `<script>` inline dans une page HTML rendue par le serveur est bloqué par le navigateur. Toute page serveur qui a besoin de JS doit charger un fichier de `/js/`. Corollaire utile : ce fichier ne peut recevoir aucune donnée par interpolation → la faire passer par `location.pathname` ou un `data-` attribut (précédent : public/js/task-confirmed.js lit l'id dans l'URL). `'style-src'` autorise `'unsafe-inline'`, les blocs `<style>` restent OK.
- ⚠️ NE PAS EMPRUNTER UNE CLASSE CSS SANS VÉRIFIER SES HANDLERS (constaté fil V3, 30/07) : réutiliser `.param-add` pour un nouveau bouton l'aurait branché sur le handler délégué de site.js qui appelle `togglePicker()` et pose `add.hidden = true` → le bouton disparaissait au premier clic. Réflexe : `grep "closest('.<classe>')" public/js/*.js` avant de réutiliser. Quand seul le style est voulu, GROUPER le sélecteur (`.param-add, .task-config { … }`) plutôt que d'ajouter la classe — même rendu, une seule source de style, zéro collision.

## Liste de courses — clés & souscriptions (débloquent des sources déjà codées)

1. ~~Cyclones OM~~ FAIT (activée 25/07 via flux ZIP DPVigilance /vigilanceom/flux/dernier, clé METEOFRANCE_API_KEY commune métropole+OM — pas d'URL/clé dédiée finalement). Extension future candidate : Polynésie/Nouvelle-Calédonie (endpoints JSON séparés).
2. MF DPBRA (avalanche, avant l'hiver) · ~~3. MF Forêts~~ **SANS OBJET (29/07)** : aucune clé n'était nécessaire — le CSV public Météo-France (S3, licence lov2) couvre tout, cf. fil « veilles paramétrées ». Leçon transposable : avant de souscrire une clé, chercher le jeu open data équivalent sur data.gouv.fr (le portail API et l'open data ne servent pas toujours la même donnée) — candidat immédiat : re-tester MF DPBRA (avalanche) sous cet angle avant toute souscription. · 4. Atmo Data (pollens+pollution — inscription à validation manuelle, la lancer tôt ; NB : pollens/qualite-air tournent DÉJÀ sans clé via le WFS open data d'Atmo France) · 5. SNCF_API_KEY · 6. RTE Tempo (portail, avant novembre).

## Roadmap

1. Chantier upcoming() public (toutes-combinaisons jours-feries + fetes-nationales) + passes à clés. (Points UX menu mobile scroll + partage mes-decks : CLOS.)
2. ONBOARDING première visite (dernier volet densité : 3 questions dans le hero → grille personnalisée avant connexion → packs suggérés — cadrage fait, jamais lancé).
3. LANCEMENT (chantier jamais ouvert — discussion stratégique à avoir : README, ~~dons /soutenir à activer~~ FAIT (PayPal+Ko-fi en prod), RGPD final, /le-point en vitrine, canaux : communautés dev pour OpenAlert, locales pour vigilances, activer Robot 3). Le produit est prêt pour des yeux extérieurs.
   POSITIONNEMENT vs apps municipales (PanneauPocket/IntraMuros/Illiwap, exploré 24/07) : PAS des concurrents directs. Leur contenu = rédigé à la main par chaque mairie (payant 180€/an+), hyper-local, non structuré, zéro paramétrage fin côté habitant. LBA = agrégation machine de sources officielles nationales/thématiques, gratuit, open source, paramétrable → complémentaire, pas substituable (pas de flux ouvert exploitable côté leur contenu). Pitch : « PanneauPocket = la vie de votre commune racontée par votre mairie ; LaBonneAlerte = tout le reste ». Marché validé : 48% des Français utilisent une app officielle de collectivité (+39 pts depuis 2013, Epiceum/Harris 2024) → appétence prouvée. Opportunité LONG TERME (V3, PAS maintenant) : OpenAlert comme émetteur pour mairies (gratuit vs abonnement payant) — ne pas ouvrir avant le cœur de cible « veilles citoyennes ».
4. Lots 2-5 audit au fil de l'eau.
5. V3 « veilles citoyennes » (3 familles : datées d'abord — rappel-personnalise en préfigure la mécanique ; maintenues ; surveillées — veille-rss idem ; badge community, espace séparé, pré-modération + charte, compte obligatoire, display_name posé). ➜ Famille **« maintenues » LIVRÉE 30/07** : carte tache-echeance-glissante, type `user-task` (cf. section de fil dédiée) — activée en base de TEST, activation prod = geste manuel.
6. App native Android (Expo, FCM), PWA en pont. Apple Sign-In si iOS.

## TODO calendaires datés (rechargements annuels de sources)

- **tour-de-france-passage — octobre 2026** : recharger le parcours à chaque annonce officielle (octobre N-1 pour l'édition N). Transcription letour.fr / ASO OBLIGATOIRE d'une source consultée (convention anti-hallucination type Bison Futé) ; jamais de parcours de mémoire ; sans source consultable → ne rien remplir. Limite constatée 25/07/2026 : les dépts traversés par étape ne sont pas publiés de façon exploitable (seules villes départ/arrivée + régions), et letour.fr n'est pas récupérable par l'outillage → config restée vide, édition 2026 non rechargée (sans objet à 2 j de l'arrivée). + créer **tdf-villes-etapes** à la même échéance (octobre 2026) — conception TRANCHÉE : source paramétrée type **commune** (résolveur commune-insee, `multiple:true`) ; config = transcription officielle `{ ville, insee, role: 'depart'|'arrivee', etape, date }` + date de l'annonce ; **DEUX alertes par ville-étape** : (1) à l'annonce (fenêtre courte après transcription, « votre ville accueille le Tour 2027 »), (2) approche **J-2** avant l'étape ; **pas de veille d'état, pas de broadcast** — simple calendrier transcrit ; **coexiste** avec tour-de-france-passage (dépt) : hyper-local (ville) vs filet (département). Aucune implémentation avant l'annonce du parcours 2027.

## Prochain fil
FILE DES CHANTIERS (ordre pressenti, mise à jour 25/07) :
1. PERSISTANCE refs — Lot 2 (scalaires) puis Lot 3 (Sets cappés) : mécanisme prouvé en prod, généralisation aux ~14 sources restantes (liste dans la section Dette). Petites touches à faible risque (opt-in).
2. GÉNÉRALISATION AUTOCOMPLETE (combobox dynamic-enum) — candidat n°1 : **vigieau** (saisie INSEE brut « 05061 » à la main, hostile au grand public) ; puis champs type commune/commune-coords (proposer pendant la frappe). Brique front déjà généralisée, à rebrancher.
3. TESTS VERSIONNÉS — `npm test` minimal (node:test) : parser panneaupocket, resolveLabel, decideTransition, validations d'URL. Filet de régression aujourd'hui INEXISTANT (les « tests » des rapports = scripts jetables).
4. TOUR DE FRANCE — octobre 2026 (annonce parcours 2027) : recharger tour-de-france-passage (parcours par dépt) ET créer **tdf-villes-etapes** (conception tranchée, cf. section TODO calendaires). Transcription letour.fr/ASO OBLIGATOIRE.
5. CYCLONES — extension Polynésie/Nouvelle-Calédonie (endpoints JSON séparés, hors flux vigilanceom).
6. VAGUES DE SOURCES — XXL 1, XXL 2, 3XL, v2-only, francophonie/plus-value (en jachère, non définies).
7. STREAMING phase 2 (SPA anti-bot → headless requis).
8. ÉTAPE B — checked_at (chantier plateforme).
Hérités d'avant le fil (toujours ouverts) : ONBOARDING première visite (3 questions hero) ; PHASE DE LANCEMENT (README, RGPD final, /le-point vitrine, activer Robot 3) ; DÉBRIEFING STRATÉGIQUE.
V3 « tâche à échéance » (suite — voir la section de fil dédiée) : **VUE LISTE = dette prioritaire** (list-view.js ne passe pas par cardHTML → aucun chemin vers les tâches en `view_mode: 'list'`) ; activation prod de la carte (`UPDATE sources SET enabled = true`) après test de bout en bout ; mode `counter` (UI de relevé) ; push sur « c'est fait ».
DECKS (suite — voir sections « Chantier cartes & decks » et « Fil 27/07 » pour le détail) : trancher l'étagère carrousel (réactiver/ajuster/abandonner), ~~RECO dédiée aux decks (pickReco, différée)~~ LIVRÉE au fil 30/07 (+ verso des tuiles-deck), simplification de la modale de partage (→ boutons inline, renommages), réglages --ds-fan/--ds-front (échelle carte de devant RÉSOLUE 27/07), feuilletage tactile desktop-only ?, + sujets actés communauté/réseaux sociaux/alertes déclenchées par l'utilisateur, illustrations réelles pour les 4 skins placeholder de la boutique.
Nettoyages à ne pas oublier : ~~purge ligne DB orpheline veille-artiste-spotify~~ FAIT (26/07), retrait du middleware diag IP dormant (LOG_CLIENT_IP), backlog visuel restant (.page-title éditoriales, .sup-block, factorisation .page, offline.html).

## Règle absolue — Header & menu (source unique)
Il existe UN SEUL header mobile normal et UN SEUL header PC normal, injectés par un composant/script
partagé (pas de HTML dupliqué par page). Toute nouvelle page DOIT utiliser ce composant, jamais
une variante recopiée. Voir docs/header-spec.md pour le détail figé.
HEADER — SOURCE UNIQUE (acquis, ne pas re-diverger) : le header hors-home est
injecté ENTIÈREMENT par js/header.js. Les pages hors-home ne contiennent qu'une
ancre <header><div class="wrap nav"></div></header> — AUCUN header statique, AUCUN
logo en dur. Le logo (LOGO_HTML dans header.js, avec ligature SVG) est la source
unique et alimente aussi le menu mobile (buildMenu).
UN SEUL MENU POUR TOUTE LA NAVIGATION (politique révisée le 03/08/2026 — la règle
« pages secondaires = 1re ligne seule » est ABANDONNÉE) : en desktop, toutes les pages
reçoivent la MÊME structure que le dashboard — 1re ligne + rangées de catégories
(injectées hors-home par buildCatRail dans header.js).
1re ligne PC identique partout : 🛍 Boutique · 🔔 Mes alertes · ♥ Favoris ·
Se connecter|avatar à initiale (auth via LBASession.renderHeader). PLUS de bouton de
thème (retiré du header ; window.toggleTheme sans déclencheur UI, à recâbler dans
Mon compte › Apparence). PLUS de bascule cartes/liste (#view-toggle supprimé, réglage
dans Mon compte › Apparence). Pas d'OpenAlert dans nav-right (footer + menu mobile
seulement). Le Point retiré du header desktop (media query >720px), présent dans le
menu mobile.
Exceptions : la HOME garde son header propre (index.html + mobile-header.js : rangées
catégories, KPI, condensation scroll m-scrolled) — dette assumée NON levée, 1re ligne
alignée à la main, mais rendu désormais identique ; l'ESPACE DÉVELOPPEURS
(proposer.html, body.dev) garde 1re ligne seule + thème sombre ; connexion.html et
offline.html sont volontairement sans header.
Header mobile hors-home = body.m-secondary (hamburger + recherche) — INCHANGÉ, le rail
de catégories injecté est masqué en ≤720px. Pour ajouter une page : charger theme.js +
session.js + header.js, ne mettre qu'une ancre de header.

## Règle absolue — Identité visuelle (source unique, même statut que le header)
rapports/identite-visuelle-reference.md = document de référence du visuel (audit complet :
tokens couleur, radius, typo, composants, motion, assets + erreurs classées par priorité).
Doit être TENU À JOUR et RELU avant toute création/modification de page ou d'élément visuel,
et sert de base pour ouvrir les fils de discussion sur le visuel. But : mettre fin aux
corrections graphiques manuelles récurrentes après chaque création. Règle d'or : aucune couleur
en dur (toujours un token var()), radius 24px cartes / 999px pilules, titres via .page-title
(Baloo 2), prefers-reduced-motion partout.
BACKLOG VISUEL (chantiers ouverts identifiés par l'audit, à traiter dans un futur lot dédié) :
doublon composant .sup-btn (redéfini inline dans soutenir.html alors que présent dans site.css) ;
.page h1 maison (Inter) vs token .page-title (Baloo 2 centré) sur pages éditoriales ; .sup-block
radius 20px (≠24px) ; bloc .page copié-collé dans 3 fichiers à factoriser ; offline.html hors
design-system. RÉSOLUS (fil cartes & decks) : token --danger/--danger-soft créé (tous les #dc2626
en dur remplacés) ; règle morte .pack-emoji supprimée ; fichier doublon « fond-constellations-jour
- Copie.svg » supprimé.
FAIT (cycle ma-collectivite) : le champ dynamic-enum est passé en COMBOBOX autocomplete (un seul
champ visuel), avec styles dédiés .dyn-enum/.dyn-search/.dyn-listbox/.dyn-option/.dyn-status
(tokens, rayons DS, focus violet), navigation clavier ARIA et zone aria-live — sorti du backlog.
FAIT (cycle « disposition A » — refonte visuelle du RECTO des cartes de source, skin « full-art » par
défaut ; cards.js + site.css) : fond dégradé thémé + motif SVG plein-carte (FULLART_SVG dans cards.js,
bloc isolé/remplaçable → substitution future PAR CATÉGORIE) + voile de lisibilité + double liseré
(radius 18/14). Rangée haute = pastille d'état (verre dépoli) à gauche + contrôles ronds 26px à droite
(♥ · partage · + deck TEINTÉ VIOLET · i) ; contenu ancré en bas (titre Baloo 2 20px + coche, sous-titre
violet uppercase, description, chips paramètres verre + « + ajouter » violet plein, switch). Variables
--tc-* (jour=light / nuit=dark) centralisent couleurs/opacités/voile ; ratio carte 2/3 (REMPLACE le 5/7
du fil « cartes & decks »). PÉRIMÈTRE STRICTEMENT VISUEL : aucun handler ni logique touché (classes
.card-share/.card-like/.card-add-deck/.flip-btn/.state/.switch/.param-* conservées ; flip 3D intact,
versos info/partage/deck ré-habillés même langage). Scope .grid + .deck-card → home/favoris/collection/
deck/mes-decks + aperçus deck-stack (réutilise cardHTML → hérite AUTO). Hors scope : carte « Mon compte »
(.acct-*) et aperçu dev (.preview) restent en radius 24px. site.js note() insère le retour dans .card-content.
  · DÉCISIONS/PIÈGES : (a) marqueur d'état actif RÉEL = .state.active (posé par stateFor, lu par
    cardIsActive) — AUCUNE classe sur .card → scope via :has(.state.active) (déjà employé dans site.css).
    (b) PERF : backdrop-filter(blur) retiré sous 620px (fonds translucides gardés) pour la grille dense
    ~178 cartes sur mobile. (c) tap mobile « .revealed » devient INERTE (contenu toujours visible en
    ratio fixe) — handler subsiste sans effet, à retirer proprement plus tard. (d) le radius 18/14 du
    skin full-art DÉROGE à la règle d'or « 24px cartes » ci-dessus (valeur de la référence validée) — à
    réconcilier dans identite-visuelle-reference.md (section « disposition A » ajoutée) si généralisé.
ANIMATION D'ÉTAT ACTIF « radar calme » (propre au skin full-art, site.css) : sur .state.active, le point
vert bat 1× puis chaque cercle concentrique passe au vert À TOUR DE RÔLE (centre→extérieur, JAMAIS deux
à la fois), puis long repos. Keyframes fa-blip (point) + fa-ring (cercles), cycle 5,6 s, fenêtre verte
3→11 % (~0,62 s), délais 0,62/1,34/2,06/2,78 s ; variables --fa-ring/--fa-green (par thème) + --fa-op
(opacité de repos par cercle) ; classes fa-dot/fa-c1..fa-c4 dans FULLART_SVG (ligne sismique + points
épars IMMOBILES) ; uniquement stroke/fill/opacity (pas de transform). prefers-reduced-motion couvert
(règle dédiée) ; desync inter-cartes via --fa-shift (nth-child). PRINCIPE CONSIGNÉ (commentaire CSS) :
chaque futur skin définira SA PROPRE animation d'état actif adaptée à son illustration ; NE PAS
dupliquer le « radar calme » (propre au full-art). PRINCIPE RÉALISÉ : les 9 skins boutique existent
désormais (5 teintes réutilisent le radar recoloré ; École/Arcane/Assemblée/Dresseur ont chacun leur
geste — cf. « Fil skins de cartes »).

## Règle absolue — Titre de page (h1) & en-tête détail deck
Tout h1 de page (y compris ceux rendus par JS) DOIT être `<h1 class="page-title" data-hero-done="1">`
comme toutes les pages du site (cohérence typo Baloo 2 + animation hero gérée par hero-letters.js).
Ne PAS régresser en `<h1>` nu.
DÉTAIL D'UN DECK (/mes-decks, renderDetail dans js/decks.js) : mise en page figée =
titre .page-title EN PLEINE LARGEUR, puis rangée `.deck-detail-layout` = [pile .deck-card à gauche]
| [`.deck-detail-body` à droite]. Le corps empile verticalement, dans l'ordre : src-desc (description),
.deck-detail-cats, .deck-detail-actions (coll-actions), #deck-adopt-msg, #deck-share-box — chacun
l'un SOUS l'autre, à DROITE de la pile et SOUS le titre. Responsive/mobile-first : sous 640px le
corps repasse SOUS la pile (colonne unique). CSS = .deck-detail-layout / .deck-detail-body (site.css).
Des refontes ont déjà cassé ça plusieurs fois → règle, ne pas re-diverger.

## Géoloc IP & département (RÉSOLU via Cloudflare + IPLocate — historique figé)
Parcours complet (ne pas rejouer les impasses) :
1. geoip-lite ABANDONNÉ pour le département : `area` mesure la confiance, pas l'exactitude
   (Gap→Champs-sur-Marne/93, area 20, faux de ~600 km). Un seuil `area` ne fiabilise rien.
2. IPv6 (plus précise) d'abord BLOQUÉE : Railway ne fait pas d'inbound IPv6 sur son edge
   public (endpoint *.up.railway.app sans AAAA ; « public inbound connections use IPv4 »).
3. BASCULE Cloudflare en frontal (DNS migré depuis OVH ; SSL Full strict ; apex
   labonnealerte.fr fonctionne maintenant SANS www). Cloudflare transmet cf-connecting-ip
   (vraie IP visiteur, IPv4 ou IPv6).
4. SÉCURISATION : ORIGIN_SECRET (var Railway) + Transform Rule Cloudflare injectant
   X-Origin-Secret. clientIp() ne fait confiance à cf-connecting-ip QUE si
   x-origin-secret == ORIGIN_SECRET (sinon repli x-forwarded-for filtré CGNAT) → empêche
   la falsification via l'URL Railway directe. Avertissement throttlé si tentative.
5. RÉSOLUTION FINALE : IPLocate (postal_code EXACT → département ; subdivision → region ;
   city → ville, en UN appel non bloquant, timeout court, clé IPLOCATE_APIKEY). Dérivation
   postal→dept dans profile-autofill.js (métropole 2 chiffres, Corse 2A/2B par plage,
   DROM 971..976), validée par isValidDepartement. Toujours « vide > faux » (hors FR /
   postal manquant / IPLocate down → NULL). Écriture profil UNIQUEMENT (departement/region/
   ville + *_source='auto'), JAMAIS dans subscriptions/source_param_states → aucune
   activation d'alerte automatique. Champs éditables dans Mon compte (→ *_source='manual').
Colonnes : country/departement/region/ville + *_source ('auto' inscription | 'manual' Mon
compte | NULL inconnu ; autofill ne remplit que si valeur NULL ET source NULL → un champ
vidé à la main n'est jamais re-deviné). server/departements-geo.js (plus-proche-voisin 101
centroïdes) reste DORMANT (postal exact d'IPLocate suffit), conservé pour usage futur.
⚠️ Middleware diag temporaire dans index.js (LOG_CLIENT_IP=1 → [ip-diag] + [ip-geo-diag])
TOUJOURS EN PLACE (dormant par défaut) — à retirer au prochain nettoyage.