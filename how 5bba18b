[33mcommit 5bba18bc3b77d388677cffee1bac7c63a279297d[m[33m ([m[1;36mHEAD[m[33m -> [m[1;32mmain[m[33m)[m
Author: Hugo <hugo@vialjaime.fr>
Date:   Sun Jul 26 11:48:38 2026 +0200

    Grille kiosque: plancher carte 340->300 (auto-fill), 3 colonnes stables 1024-1920, cartes 313-385px, gap inchange

[1mdiff --git a/public/css/site.css b/public/css/site.css[m
[1mindex f154b0f..abef0ab 100644[m
[1m--- a/public/css/site.css[m
[1m+++ b/public/css/site.css[m
[36m@@ -209,18 +209,20 @@[m [mhtml[data-theme="dark"] .search { border: 1px solid color-mix(in srgb, var(--mut[m
    à ~644px et écrasait les autres — visible seulement au filtre car cette carte est[m
    paginée en vue « Toutes ». `minmax(0,1fr)` force des colonnes égales, le contenu[m
    pouvant rétrécir/déborder proprement (voir aussi `.card { min-width:0 }`). */[m
[31m-/* Format « carte a jouer » : la grille est ADAPTATIVE (le nombre de colonnes se[m
[31m-   recalcule selon la largeur), plutot que 3 colonnes fixes. Largeur de carte cible[m
[31m-   ~340px sur desktop → 3 colonnes sur .wrap (1240px), 2 en tablette, 1 en mobile.[m
[31m-   La carte garde son ratio (voir .card-face). minmax(min(340px,100%),1fr) preserve le[m
[31m-   garde-fou « vie locale » (colonnes qui ne gonflent pas au min-content d'un <select>). */[m
[32m+[m[32m/* Format « carte a jouer » : la grille est ADAPTATIVE (auto-fill, le nombre de colonnes se[m
[32m+[m[32m   recalcule selon la largeur), plutot que 3 colonnes fixes. Plancher de carte = 300px →[m
[32m+[m[32m   3 colonnes sur tout le desktop (1024→1920), 2 en tablette, 1 en mobile. Les cartes[m
[32m+[m[32m   REMPLISSENT (1fr) : largeur reelle mesuree = 313px (~1024) a 385px (>=1280), dans la[m
[32m+[m[32m   fourchette voulue (280-420). minmax(min(300px,100%),1fr) preserve le garde-fou « vie[m
[32m+[m[32m   locale » (colonnes qui ne gonflent pas au min-content d'un <select> long). */[m
 /* BUG conteneur (corrige) : body est flex-column (footer colle en bas) et .wrap a[m
    margin:0 auto ; une marge auto sur l'axe transverse d'un flex item DESACTIVE le stretch[m
    → main#alertes retrecissait a son contenu (~675px) et auto-fill s'effondrait a 2 colonnes.[m
[31m-   width:100% redonne une largeur DEFINIE (cappee par max-width:1240) → etirement correct,[m
[31m-   auto-fill calcule 3 colonnes. Meme correctif sur .decks-main (Mes decks). */[m
[32m+[m[32m   width:100% redonne une largeur DEFINIE (cappee par max-width:1240 → grille 1192px) →[m
[32m+[m[32m   etirement correct. 4 colonnes ne tiennent JAMAIS (4x300=1200 > 1192) : le gap n'a donc[m
[32m+[m[32m   pas a etre gonfle pour bloquer la 4e colonne, il reste a 18px. Meme fix sur .decks-main. */[m
 main#alertes { width: 100%; }[m
[31m-.grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(min(340px, 100%), 1fr)); gap: 18px; padding-top: 2rem; padding-bottom: 26px; grid-auto-rows: auto; align-content: start; }[m
[32m+[m[32m.grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(min(300px, 100%), 1fr)); gap: 18px; padding-top: 2rem; padding-bottom: 26px; grid-auto-rows: auto; align-content: start; }[m
 /* Mobile : UNE carte par rangee (format « carte a jouer » pleine largeur, ratio conserve).[m
    minmax(0,1fr) → la carte se partage la largeur sans jamais deborder, meme sur les tres[m
    petits ecrans (~320px). */[m
